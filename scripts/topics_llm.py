# -*- coding: utf-8 -*-
"""LLM systems topics rendered by gen_guide.py."""

TOPICS = [{'category': 'llm', 'slug': 'llm-serving', 'title_en': 'LLM Serving at Scale', 'title_zh': '大规模 LLM 推理', 'lead_en': 'Prefill vs decode, PagedAttention, continuous batching, speculative decoding, FlashAttention — how vLLM, TensorRT-LLM, and SGLang squeeze tokens/second out of an H100.', 'lead_zh': 'Prefill vs Decode、PagedAttention、连续批、投机解码、FlashAttention——vLLM、TensorRT-LLM、SGLang 如何从 H100 榜出 token/秒。', 'tags': ['vLLM', 'PagedAttention', 'Speculative decoding', 'TTFT/ITL'], 'refs': ['vLLM (Kwon 2023)', 'FlashAttention (Dao 2022)', 'Speculative Decoding (Leviathan 2023)'], 'toc_en': ['Why LLM serving is different', 'Prefill vs decode: two workloads', 'KV cache, PagedAttention, prefix caching', 'FlashAttention, speculative decoding, quantization', 'Scheduling, autoscaling, failure modes', 'Interview checklist'], 'toc_zh': ['为什么 LLM 推理不一样', 'Prefill vs Decode：两种工作负载', 'KV cache、PagedAttention、前缀缓存', 'FlashAttention、投机解码、量化', '调度、自动扩缩、故障模式', '面试清单'], 'body_en': '\n<h2 id="s1">Why LLM serving is different</h2>\n<p>Classic web serving is "compute small response, return". LLM serving is "stream thousands of autoregressive tokens, each requiring a full forward pass through 70B parameters, while sharing a GPU with dozens of other requests". The constraints:</p>\n<ul>\n  <li><strong>Memory-bound decode</strong>. Generating one token touches every parameter. For Llama-70B fp16 = 140 GB weights. On an H100 (HBM 3.35 TB/s), one forward pass reads 140 GB / 3350 GB/s ≈ 42 ms — that is your theoretical lower bound per token on a single GPU. You need tensor parallelism across multiple GPUs to get faster.</li>\n  <li><strong>KV cache explosion</strong>. For each in-flight request, you keep the attention Key/Value tensors for every prior token. Formula: <code>kv_bytes ≈ 2 * layers * heads * head_dim * seq_len * dtype_bytes</code>. Llama-70B at 4096 tokens ≈ 1.3 GB per request. An 80 GB H100 fits only ~50 concurrent requests at that context.</li>\n  <li><strong>TTFT vs ITL trade-off</strong>. Time-to-first-token (TTFT) depends on prompt length (prefill). Inter-token latency (ITL) depends on decode speed. A chat app cares about both; a batch summarizer only cares about throughput.</li>\n</ul>\n<p>Interview number to memorize: a 7B model in fp16 is ~14 GB; 13B ≈ 26 GB; 70B ≈ 140 GB; 405B ≈ 810 GB. Quantization (fp8, int4) cuts this by 2–4×.</p>\n<div class="callout tip"><h4>Source cross-reference</h4><p>Read the <em>vLLM paper</em> (Kwon et al., 2023, "Efficient Memory Management for Large Language Model Serving with PagedAttention"), <em>FlashAttention</em> (Dao 2022), and <em>Speculative Decoding</em> (Leviathan et al. 2023, "Fast Inference from Transformers via Speculative Decoding"). These three papers explain 80% of every modern inference server.</p></div>\n\n<h2 id="s2">Prefill vs decode: two different workloads</h2>\n<p>A single generation is two phases:</p>\n<ol>\n  <li><strong>Prefill</strong>: consume the prompt in one parallel forward pass. Compute-bound (matmuls over the whole prompt); dominates TTFT. Latency scales roughly linearly with prompt length.</li>\n  <li><strong>Decode</strong>: generate output tokens one at a time. Memory-bound (each step reads the full weight matrix to produce one token). Dominates total latency for long outputs.</li>\n</ol>\n<p>These phases have opposite optimal batch sizes. Prefill saturates the GPU at batch 1 (the prompt itself is already a big matmul). Decode needs batch &gt;16 to saturate memory bandwidth efficiently. Mixing them naively wastes both. Two solutions in modern servers:</p>\n<ul>\n  <li><strong>Disaggregated prefill/decode</strong> (DistServe, TensorRT-LLM): separate GPU pools for prefill and decode. KV cache transferred over NVLink/IB once. Improves both TTFT and ITL at the cost of operational complexity.</li>\n  <li><strong>Chunked prefill</strong> (vLLM, SGLang): split a long prompt into chunks and interleave with decode batches. One pool, no transfer, but harder scheduling. Sarathi-Serve paper shows 2–3× higher throughput than naive schedulers.</li>\n</ul>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  R[Request] --&gt; S[Scheduler]\n  S --&gt;|prompt| P[Prefill GPU<br/>compute-bound]\n  P --&gt;|KV cache| D[Decode GPU pool<br/>continuous batching]\n  D --&gt; T[Token stream]\n  T --&gt; C[Client SSE]\n</pre></div>\n\n<h2 id="s3">KV cache, PagedAttention, prefix caching</h2>\n<p>The KV cache is the single largest lever in LLM serving. Three techniques matter:</p>\n<h3>PagedAttention (vLLM)</h3>\n<p>Classic KV cache allocates one contiguous buffer per request sized to <code>max_seq_len</code>. Result: 60–80% of that memory is wasted on padding. PagedAttention, modeled on OS virtual memory, breaks KV into 16-token <em>pages</em>. A request holds a page table; pages are allocated on demand. vLLM\'s paper reports 2–4× higher throughput vs HuggingFace pipelines at equal latency.</p>\n<h3>Prefix caching</h3>\n<p>If many requests share a long system prompt (think 2000-token Claude system prompt), you can cache that prefix\'s KV once. Subsequent requests skip that prefill work entirely. Anthropic explicitly exposes this as <strong>prompt caching</strong>: the first request pays full cost, subsequent requests within a 5-minute window pay ~10% for the cached prefix. OpenAI has similar automatic prefix caching for long system prompts.</p>\n<h3>Continuous batching</h3>\n<p>Static batching (wait until the batch is full, generate all together) stalls: a short request finishes in 3 tokens while the batch must wait for 1000-token companions. Continuous batching (Orca, vLLM) admits new requests every step and evicts finished ones mid-batch. Throughput improves 2–3× over static batching at the same latency.</p>\n\n<h2 id="s4">FlashAttention, speculative decoding, quantization</h2>\n<h3>FlashAttention (Dao 2022, v2 2023, v3 2024)</h3>\n<p>Standard attention materializes an N×N matrix — at N=8k, that\'s 256 MB per head. FlashAttention tiles the computation to keep it in SRAM, fusing softmax, matmul, and masking in one kernel. Result: 2–4× faster attention, linear memory in sequence length. Every production engine uses it (vLLM, TensorRT-LLM, xFormers, SGLang).</p>\n<h3>Speculative decoding (Leviathan 2023)</h3>\n<p>A small "draft" model (e.g., Llama-8B) generates K=5 candidate tokens; the large target model (Llama-70B) verifies them in one forward pass. Expected acceptance rate ~70%, yielding 2–3× decode speedup with no quality loss. Variants: Medusa (multi-head draft), EAGLE, lookahead decoding.</p>\n<h3>Quantization</h3>\n<ul>\n  <li><strong>fp8</strong> (H100 native): 2× vs fp16, tiny quality drop for most models.</li>\n  <li><strong>int4 (GPTQ, AWQ)</strong>: 4× memory reduction, 1–2% benchmark loss; used by most open-weights serving (llama.cpp, Exllama).</li>\n  <li><strong>int8 with smoothquant</strong>: popular for latency-critical smaller models.</li>\n</ul>\n<div class="callout openai"><h4>OpenAI-specific</h4><p>OpenAI runs GPT-4-class models on clusters of H100s with custom kernels likely derived from FlashAttention-3 and TensorRT-LLM. Observable behaviors: strong prefix caching (cached system prompts run ~50% faster), aggressive streaming, and per-request timeouts. Their per-token pricing reflects the prefill vs decode cost split.</p></div>\n<div class="callout anthropic"><h4>Anthropic-specific</h4><p>Anthropic\'s API surfaces <em>prompt caching</em> as a first-class feature with explicit cache-control blocks in the Messages API. Cached tokens are billed at ~10% of input rate. This is unique — OpenAI caches but doesn\'t give you control over cache breakpoints. In interviews, mention that Anthropic\'s public cache-hit rate on long Claude Code system prompts exceeds 90%.</p></div>\n\n<h2 id="s5">Scheduling, autoscaling, failure modes</h2>\n<h3>Scheduling signals</h3>\n<p>You do <em>not</em> autoscale LLM inference on GPU utilization — an idle GPU with 50 queued requests looks "90% utilized" but is starved. Right signals:</p>\n<ul>\n  <li><strong>Queue depth</strong> (requests waiting for prefill).</li>\n  <li><strong>TTFT p95</strong> — scale up when it exceeds SLO (e.g., &gt;1s).</li>\n  <li><strong>KV cache utilization</strong> — when running hot, reject or queue new requests.</li>\n</ul>\n<h3>Architecture</h3>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  C[Client] --&gt; LB[Global LB]\n  LB --&gt; RT[Router<br/>model + region]\n  RT --&gt; Q[Priority queue<br/>by tier]\n  Q --&gt; SCH[Scheduler<br/>continuous batching]\n  SCH --&gt; P1[Prefill pool H100x8]\n  SCH --&gt; D1[Decode pool H100x8]\n  P1 --&gt;|KV page table| D1\n  D1 --&gt; SSE[Token streamer]\n  SSE --&gt; C\n  M[Metrics: TTFT, ITL, QD] --&gt; A[Autoscaler]\n  A --&gt; P1\n  A --&gt; D1\n</pre></div>\n<h3>Failure modes and anti-patterns</h3>\n<div class="callout warn"><h4>Anti-patterns</h4>\n<ul>\n  <li><strong>Static batching</strong> in a chat product. You\'ll eat 10× the latency of your shortest request.</li>\n  <li><strong>Autoscale on GPU util</strong>. Memory-bound decode pegs HBM but not SM; metric lies.</li>\n  <li><strong>Unbounded max_tokens</strong>. A user sets <code>max_tokens=32k</code>, holds KV cache for 5 minutes, starves everyone else. Cap and enforce.</li>\n  <li><strong>Shared prefill+decode pool at batch 1</strong>. Long prompts block decode steps; TTFT and ITL both degrade.</li>\n  <li><strong>No draining on deploy</strong>. Killing a pod mid-stream drops hundreds of open SSE connections; use lame-duck mode.</li>\n</ul></div>\n<p>Systems to name in interviews: <strong>vLLM</strong> (open-source, PagedAttention), <strong>TensorRT-LLM</strong> (NVIDIA, fastest on H100), <strong>SGLang</strong> (structured gen + RadixAttention for prefix sharing), <strong>TGI</strong> (HuggingFace), <strong>DeepSpeed-MII</strong>, <strong>Ray Serve + vLLM</strong> (the common startup stack).</p>\n\n<h2 id="s6">Interview checklist</h2>\n<ol>\n  <li>Clarify: model size, max context, target TTFT, target throughput, streaming or batch.</li>\n  <li>Back-of-envelope: weights memory, KV cache per request, concurrent request ceiling.</li>\n  <li>Pick engine (vLLM default; TensorRT-LLM if latency-critical).</li>\n  <li>Continuous batching + PagedAttention + FlashAttention (all three).</li>\n  <li>Prefix caching / prompt caching for repeated system prompts.</li>\n  <li>Disaggregate or chunked-prefill for large prompt mix.</li>\n  <li>Quantization if memory-bound (fp8 or int4/AWQ).</li>\n  <li>Speculative decoding for quality-matched speedup.</li>\n  <li>Autoscale on queue depth + TTFT p95, not GPU util.</li>\n  <li>Per-request cap on <code>max_tokens</code>; lame-duck on deploy; SSE backpressure.</li>\n</ol>\n', 'body_zh': '\n<h2 id="s1">为什么 LLM 推理不一样</h2>\n<p>传统 Web 服务是"算个小响应返回"。LLM 推理是"流式吐出数千个自回归 token，每个都要经过 70B 参数的完整前向，与几十个请求共享 GPU"。约束：</p>\n<ul>\n  <li><strong>Decode 受内存带宽限制</strong>。生成一个 token 要访问全部参数。Llama-70B fp16 = 140 GB 权重。H100（HBM 3.35 TB/s）单次前向 140 GB / 3350 GB/s ≈ 42 ms——这是单 GPU 每 token 的理论下界。想更快必须 tensor parallel。</li>\n  <li><strong>KV 缓存爆炸</strong>。每个 in-flight 请求保留所有过去 token 的 Key/Value。公式：<code>kv_bytes ≈ 2 * layers * heads * head_dim * seq_len * dtype_bytes</code>。Llama-70B 4096 token ≈ 1.3 GB/请求。80 GB H100 只能同时放 ~50 个请求。</li>\n  <li><strong>TTFT vs ITL 权衡</strong>。首 token 时间（TTFT）依赖 prompt 长度（prefill）；token 间延迟（ITL）依赖 decode 速度。聊天两者都要；批量摘要只看吞吐。</li>\n</ul>\n<p>要背的数：7B fp16 ≈ 14 GB；13B ≈ 26 GB；70B ≈ 140 GB；405B ≈ 810 GB。量化（fp8、int4）能减 2-4 倍。</p>\n<div class="callout tip"><h4>参考来源</h4><p>读 vLLM 论文（Kwon et al. 2023，PagedAttention）、FlashAttention（Dao 2022）、Speculative Decoding（Leviathan et al. 2023）。这三篇解释了每个现代推理服务器 80% 的机制。</p></div>\n\n<h2 id="s2">Prefill vs Decode：两种工作负载</h2>\n<p>一次生成分两阶段：</p>\n<ol>\n  <li><strong>Prefill</strong>：一次并行前向消费整个 prompt。计算瓶颈（对整条 prompt 做大矩阵乘）；决定 TTFT。延迟近似线性于 prompt 长度。</li>\n  <li><strong>Decode</strong>：一次一个 token。内存瓶颈（每步要读完整权重矩阵生成一个 token）。决定长输出总延迟。</li>\n</ol>\n<p>两阶段最佳 batch 相反。Prefill 在 batch=1 就打满 GPU（prompt 本身已是个大矩阵乘）。Decode 需要 batch &gt;16 才能打满 HBM。简单混在一起两败俱伤。现代服务器两种方案：</p>\n<ul>\n  <li><strong>分离 prefill/decode</strong>（DistServe、TensorRT-LLM）：prefill 和 decode 用独立 GPU 池，KV cache 通过 NVLink/IB 传一次。TTFT 和 ITL 都好，运维复杂度加码。</li>\n  <li><strong>Chunked prefill</strong>（vLLM、SGLang）：长 prompt 切块，与 decode 批交错。一个池、不用传，但调度更难。Sarathi-Serve 论文显示比朴素调度器 2-3 倍吞吐。</li>\n</ul>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  R[请求] --&gt; S[调度器]\n  S --&gt;|prompt| P[Prefill GPU<br/>计算瓶颈]\n  P --&gt;|KV cache| D[Decode GPU 池<br/>连续批]\n  D --&gt; T[token 流]\n  T --&gt; C[Client SSE]\n</pre></div>\n\n<h2 id="s3">KV cache、PagedAttention、前缀缓存</h2>\n<p>KV cache 是 LLM 推理最大杠杆。三个关键技术：</p>\n<h3>PagedAttention (vLLM)</h3>\n<p>传统 KV cache 每请求分配一段连续 buffer 按 <code>max_seq_len</code> 大小。结果：60-80% 内存浪费在 padding。PagedAttention 借鉴 OS 虚拟内存，把 KV 切成 16 token 的<em>页</em>。请求持有页表，按需分配。vLLM 论文报告同延迟下 2-4 倍吞吐于 HuggingFace pipeline。</p>\n<h3>前缀缓存</h3>\n<p>如果很多请求共享长 system prompt（想象 2000 token 的 Claude system prompt），可缓存该前缀的 KV 一次。后续请求完全跳过此 prefill。Anthropic 显式暴露为 <strong>prompt caching</strong>：首请求付全价，5 分钟窗口内后续只付 ~10% 缓存费。OpenAI 对长 system prompt 自动缓存。</p>\n<h3>连续批</h3>\n<p>静态批（等 batch 满了一起生成）会卡：3 token 就完的短请求要等 1000 token 的同伴。连续批（Orca、vLLM）每步可接入新请求、淘汰已完成者。吞吐比静态批在相同延迟下高 2-3 倍。</p>\n\n<h2 id="s4">FlashAttention、投机解码、量化</h2>\n<h3>FlashAttention (Dao 2022、v2 2023、v3 2024)</h3>\n<p>标准 attention 会物化 N×N 矩阵——N=8k 时每 head 256 MB。FlashAttention 把计算分 tile 保持在 SRAM，把 softmax、矩阵乘、mask 融在一个 kernel。结果：attention 快 2-4 倍，对序列长度线性内存。所有生产引擎都用（vLLM、TensorRT-LLM、xFormers、SGLang）。</p>\n<h3>投机解码（Leviathan 2023）</h3>\n<p>小"草稿"模型（例如 Llama-8B）生成 K=5 候选 token，大目标模型（Llama-70B）一次前向验证。期望接受率 ~70%，无质量损失 2-3 倍 decode 加速。变种：Medusa（多头草稿）、EAGLE、lookahead decoding。</p>\n<h3>量化</h3>\n<ul>\n  <li><strong>fp8</strong>（H100 原生）：对 fp16 2 倍，大多数模型质量损失微小。</li>\n  <li><strong>int4（GPTQ、AWQ）</strong>：4 倍内存缩减，benchmark 损失 1-2%；开源权重推理（llama.cpp、Exllama）主流。</li>\n  <li><strong>int8 + smoothquant</strong>：延迟敏感的中小模型。</li>\n</ul>\n<div class="callout openai"><h4>OpenAI 细节</h4><p>OpenAI 在 H100 集群上跑 GPT-4 级模型，可能用基于 FlashAttention-3 与 TensorRT-LLM 的定制 kernel。可观察到：强前缀缓存（缓存过的 system prompt 约 50% 加速）、积极流式、按请求超时。按 token 计价反映了 prefill vs decode 成本分裂。</p></div>\n<div class="callout anthropic"><h4>Anthropic 细节</h4><p>Anthropic API 把 <em>prompt caching</em> 作为一等功能，Messages API 允许显式 cache_control 块。命中的缓存 token ~10% 输入价。这点独特——OpenAI 缓存但不让你控制边界。面试里可以提 Anthropic 公开 Claude Code 长 system prompt 缓存命中率 &gt;90%。</p></div>\n\n<h2 id="s5">调度、自动扩缩、故障模式</h2>\n<h3>调度信号</h3>\n<p>LLM 推理<em>不能</em>按 GPU 利用率扩缩——50 个排队但 GPU 空闲时利用率显示"90%"实际是饿死。正确信号：</p>\n<ul>\n  <li><strong>队列深度</strong>（等 prefill 的请求数）。</li>\n  <li><strong>TTFT p95</strong>——超 SLO（例如 &gt;1s）就扩。</li>\n  <li><strong>KV 缓存利用率</strong>——接近满时拒绝或排队。</li>\n</ul>\n<h3>架构</h3>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  C[Client] --&gt; LB[Global LB]\n  LB --&gt; RT[Router<br/>model + region]\n  RT --&gt; Q[按 tier 优先队列]\n  Q --&gt; SCH[调度器<br/>连续批]\n  SCH --&gt; P1[Prefill 池 H100x8]\n  SCH --&gt; D1[Decode 池 H100x8]\n  P1 --&gt;|KV 页表| D1\n  D1 --&gt; SSE[token 流]\n  SSE --&gt; C\n  M[指标: TTFT, ITL, QD] --&gt; A[自动扩缩器]\n  A --&gt; P1\n  A --&gt; D1\n</pre></div>\n<h3>故障与反模式</h3>\n<div class="callout warn"><h4>反模式</h4>\n<ul>\n  <li><strong>聊天产品用静态批</strong>：最短请求延迟被放大 10 倍。</li>\n  <li><strong>按 GPU util 扩缩</strong>：decode 内存瓶颈、HBM 满但 SM 没满，指标骗人。</li>\n  <li><strong>不限 max_tokens</strong>：用户设 32k、KV cache 占 5 分钟、饿死他人。必须上限并强制。</li>\n  <li><strong>共享 prefill+decode 单池 batch=1</strong>：长 prompt 卡住 decode 步，TTFT 和 ITL 双退化。</li>\n  <li><strong>部署不 drain</strong>：杀 pod 中断数百 SSE 连接，得用 lame-duck。</li>\n</ul></div>\n<p>面试提名的系统：<strong>vLLM</strong>（开源、PagedAttention）、<strong>TensorRT-LLM</strong>（NVIDIA、H100 最快）、<strong>SGLang</strong>（结构化生成 + RadixAttention 前缀共享）、<strong>TGI</strong>、<strong>DeepSpeed-MII</strong>、<strong>Ray Serve + vLLM</strong>（创业公司常见栈）。</p>\n\n<h2 id="s6">面试清单</h2>\n<ol>\n  <li>澄清：模型大小、最大上下文、目标 TTFT、目标吞吐、流式还是批量。</li>\n  <li>估算：权重内存、每请求 KV、并发请求上限。</li>\n  <li>选引擎（vLLM 默认；延迟严苛用 TensorRT-LLM）。</li>\n  <li>连续批 + PagedAttention + FlashAttention（全上）。</li>\n  <li>重复 system prompt 上前缀/prompt 缓存。</li>\n  <li>prompt 混合大时分离或 chunked prefill。</li>\n  <li>内存瓶颈就量化（fp8 或 int4/AWQ）。</li>\n  <li>投机解码获质量等同加速。</li>\n  <li>按队列深度 + TTFT p95 扩缩，别用 GPU util。</li>\n  <li>max_tokens 上限、部署 lame-duck、SSE 背压。</li>\n</ol>\n', 'links': [('Arena question', '真题', '../../arena/questions/a11-llm-inference-service.html', 'A11 · LLM Inference Service', 'A11 · LLM 推理服务'), ('Arena question', '真题', '../../arena/questions/a12-gpu-batching.html', 'A12 · GPU Batching', 'A12 · GPU 批处理'), ('Arena question', '真题', '../../arena/questions/a16-low-latency-inference-api.html', 'A16 · Low-Latency Inference', 'A16 · 低延迟推理'), ('Next topic', '下一主题', 'rag.html', 'Retrieval-Augmented Generation', '检索增强生成'), ('Related training', '相关训练', 'distributed-training.html', 'Distributed Training', '分布式训练')]}, {'category': 'llm', 'slug': 'rag', 'title_en': 'Retrieval-Augmented Generation', 'title_zh': '检索增强生成 (RAG)', 'lead_en': 'Chunking, hybrid BM25+dense retrieval, cross-encoder rerank, HyDE, citations, and multi-modal RAG — the production patterns behind enterprise-grade knowledge bots.', 'lead_zh': '切块、BM25+dense 混合检索、交叉编码器重排、HyDE、引用、多模态 RAG——企业级知识助手的生产模式。', 'tags': ['Hybrid retrieval', 'Reranking', 'HyDE', 'Contextual retrieval'], 'refs': ['Gulli Ch.14', 'RAG paper (Lewis 2020)', 'HyDE (Gao 2022)'], 'toc_en': ['What RAG is and when to use it', 'Chunking: the part everyone gets wrong', 'Hybrid retrieval + reranking', 'Query-side tricks: HyDE, decomposition, multi-hop', 'Grounding, citations, and multi-modal', 'Evaluation and interview checklist'], 'toc_zh': ['RAG 是什么、何时用', '切块：最容易做错的地方', '混合检索 + 重排', 'Query 端技巧：HyDE、分解、多跳', 'grounding、引用、多模态', '评估与面试清单'], 'body_en': '\n<h2 id="s1">What RAG is and when to use it</h2>\n<p>Retrieval-Augmented Generation (RAG) grounds an LLM\'s answer in documents it did not see at training. The canonical interview framing: "given a corpus of N million docs, answer questions with citations, under 500ms, without finetuning the model".</p>\n<p>Use RAG when you need: (1) up-to-date knowledge (the model\'s training cutoff is stale); (2) private knowledge (your wiki, Slack, customer tickets); (3) auditable citations; (4) long-tail factual precision where hallucination is unacceptable. Don\'t use RAG for reasoning-only tasks or when the answer is in the model\'s parameters anyway (simple arithmetic, general summarization).</p>\n<p>Interview numbers: a typical enterprise corpus is 1M–100M chunks of ~500 tokens each. Embeddings at 1024-dim fp16 = 2 KB/chunk, so 100M chunks = 200 GB — fits in a single-box Milvus/Qdrant with RAM overprovisioning. End-to-end budget: 50ms retrieval, 100ms rerank, 300ms generation with streaming first-token.</p>\n<div class="callout tip"><h4>Source cross-reference</h4><p>Gulli\'s <em>Agentic Design Patterns</em> Ch.14 (Knowledge Retrieval) is the canonical catalog. Read the original RAG paper (Lewis et al. 2020), Contriever/ColBERT for retrieval, and HyDE (Gao et al. 2022) for query-side tricks.</p></div>\n\n<h2 id="s2">Chunking: the part everyone gets wrong</h2>\n<p>Chunking is the single biggest quality lever — worse chunking beats better embeddings. Three strategies:</p>\n<ul>\n  <li><strong>Fixed-size</strong> (256/512/1024 tokens with 10–20% overlap). Dead simple. Works fine for narrative text; terrible for code and structured docs.</li>\n  <li><strong>Semantic</strong> (chunk at sentence/paragraph boundaries; cluster adjacent sentences by embedding distance). Best quality; more compute.</li>\n  <li><strong>Structural</strong> (split at markdown headers, code function boundaries, HTML sections). The right answer for technical documentation, code, and wikis.</li>\n  <li><strong>Sliding window with "parent doc" retrieval</strong>: store small chunks for retrieval, return the full parent section at generation time. Best of both.</li>\n</ul>\n<p>Number rule: optimize chunk size to match the expected answer granularity. FAQ-style → 200 tokens. Dense technical docs → 500–800. Conversations → by turn.</p>\n<div class="callout warn"><h4>Anti-pattern</h4><p>One chunk size for all corpora. A single 512-token chunk cuts PDF tables in half and destroys code. Always inspect chunks by eye on 20 random documents before deploying.</p></div>\n\n<h2 id="s3">Hybrid retrieval + reranking</h2>\n<p>Production RAG almost always uses <strong>hybrid retrieval</strong>:</p>\n<ol>\n  <li><strong>BM25 / keyword</strong> (Elasticsearch, OpenSearch). Catches exact matches — SKUs, error codes, names. Vector search misses these.</li>\n  <li><strong>Dense</strong> (embedding similarity, e.g., cosine over bge-large, text-embedding-3-small). Catches semantic matches.</li>\n  <li><strong>Reciprocal Rank Fusion (RRF)</strong> merges the two ranked lists: <code>score(d) = Σ 1/(k + rank_i(d))</code> with k=60 typical. Beats either alone by 5–15% on retrieval benchmarks.</li>\n</ol>\n<p>Then <strong>rerank</strong> with a cross-encoder (e.g., Cohere rerank, BGE-reranker, Jina). Cross-encoders score each (query, doc) pair jointly — 100× slower than bi-encoders but 10–20% NDCG lift. Pattern: retrieve 50 candidates hybrid → rerank top 50 → keep top 5 for the LLM.</p>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  Q[User query] --&gt; QE[Query encoder]\n  Q --&gt; QR[Query rewriter<br/>HyDE / decomp]\n  QR --&gt; BM[BM25 index]\n  QE --&gt; VX[Vector index<br/>HNSW]\n  BM --&gt; RRF[RRF merge]\n  VX --&gt; RRF\n  RRF --&gt; XR[Cross-encoder rerank]\n  XR --&gt; CTX[Top-5 chunks]\n  CTX --&gt; LLM[LLM + citations]\n  LLM --&gt; A[Answer + refs]\n</pre></div>\n\n<h2 id="s4">Query-side tricks: HyDE, decomposition, multi-hop</h2>\n<h3>HyDE (Hypothetical Document Embeddings)</h3>\n<p>A user query "what did we decide about paid leave?" embeds poorly because it\'s a question, but docs are statements. HyDE: ask the LLM to <em>draft</em> a fake answer, then embed that. The fake doc embedding matches real doc embeddings much better. Empirically +10–15% retrieval recall at a cost of one LLM call.</p>\n<h3>Query decomposition</h3>\n<p>Multi-part queries ("compare our Q3 revenue to Anthropic\'s Q3 revenue") embed poorly. Have the LLM split into sub-queries, retrieve for each, union the context. Standard with tool-using agents; pairs with the planner/executor pattern (see the agentic-patterns page).</p>\n<h3>Multi-hop</h3>\n<p>"Who is the CTO of the company that acquired Figma?" requires two retrievals. Solutions: (a) LLM planner executes hop 1, then hop 2; (b) GraphRAG builds an entity graph up front so multi-hop is a graph traversal; (c) fine-tuned retriever like Self-RAG that knows when to retrieve more.</p>\n\n<h2 id="s5">Grounding, citations, and multi-modal</h2>\n<h3>Citation-grounded generation</h3>\n<p>Prompt the LLM to cite source IDs inline: <code>[doc_id:chunk_id]</code>. Post-process to verify every citation actually exists. For Anthropic-style interviews, explicitly discuss how you would <em>measure</em> grounding: train a separate NLI model to check if each output claim is entailed by the cited passage (this is what FActScore and the Bespoke-Minicheck tools do).</p>\n<h3>Long-context vs RAG</h3>\n<p>Claude 3 Opus has 200k context; Gemini 1.5 Pro has 1M. Doesn\'t RAG become obsolete? No — 1M-token prompts cost $3+ per query at fp16 pricing, dwarfing retrieval cost. Also, "needle in haystack" recall degrades past ~100k tokens. Long context <em>complements</em> RAG: retrieve coarse to fit 100k tokens, let the model sift.</p>\n<h3>Multi-modal RAG</h3>\n<p>PDFs contain tables, charts, figures. Two approaches: (1) OCR + layout parsing (LayoutLMv3, Unstructured.io), embed text per table/figure; (2) vision-language embeddings (CLIP, SigLIP, ColPali) — embed page images directly. ColPali shows that indexing page images with a late-interaction VLM beats OCR-pipelines on visual-heavy corpora.</p>\n<div class="callout openai"><h4>OpenAI-specific</h4><p>OpenAI\'s Assistants / File Search product builds RAG for you: auto-chunking (800 tokens, 400 overlap), hybrid retrieval, auto-reranking. Good for MVP, but you lose control of chunk strategy and eval — bring your own pipeline for production.</p></div>\n<div class="callout anthropic"><h4>Anthropic-specific</h4><p>Anthropic\'s "Contextual Retrieval" technique (public engineering blog) prepends a chunk-specific one-sentence context to each chunk <em>before</em> embedding. Example: "From the 2023 10-K, section on liquidity: [original chunk]". Reported 49% reduction in retrieval failures when combined with BM25 + rerank. This is the interview bait: cite it.</p></div>\n\n<h2 id="s6">Evaluation and interview checklist</h2>\n<p>You can\'t improve RAG without offline eval. Minimum viable eval harness:</p>\n<ul>\n  <li><strong>Retrieval metrics</strong>: hit@k, recall@k, MRR on a labeled set of 200 (query, gold-chunk) pairs.</li>\n  <li><strong>Grounding metrics</strong>: faithfulness (does answer only use retrieved context?), citation precision.</li>\n  <li><strong>End-to-end</strong>: LLM-as-judge on answer quality, calibrated against 50 human-rated golds. See the <a href="llm-evaluation.html">evaluation</a> page.</li>\n  <li><strong>Online</strong>: thumbs up/down, regret rate, follow-up-question rate.</li>\n</ul>\n<div class="callout warn"><h4>Anti-patterns</h4>\n<ul>\n  <li>No eval set. You\'re flying blind; every "improvement" is vibes.</li>\n  <li>Vector-only retrieval. You will miss exact-match queries. Always hybrid.</li>\n  <li>Top-k = 20 stuffed into the prompt. Cross-encoder rerank to top-5 or you dilute the LLM.</li>\n  <li>One global index for multi-tenant data. Permission leaks become compliance incidents.</li>\n  <li>Ignoring freshness. Index at write time or hourly; daily rebuilds mean stale answers.</li>\n</ul></div>\n<p><strong>Whiteboard checklist:</strong> corpus size and shape → chunking strategy → embedding model → hybrid index (BM25 + HNSW) → query rewrite (HyDE / decomp) → cross-encoder rerank → citation-grounded generation → eval harness → freshness pipeline → per-tenant permissions.</p>\n', 'body_zh': '\n<h2 id="s1">RAG 是什么、何时用</h2>\n<p>检索增强生成（RAG）让 LLM 基于训练时未见的文档作答。经典面试表述："给定 N 百万文档的语料，带引用回答问题，500ms 内，不微调模型"。</p>\n<p>何时用 RAG：(1) 需要最新知识（模型训练截止）；(2) 私有知识（你的 wiki、Slack、工单）；(3) 需可审计的引用；(4) 长尾事实精准、无法容忍幻觉。不要用 RAG 做纯推理任务或答案本就在模型参数里的事（简单算术、泛摘要）。</p>\n<p>面试数：企业典型语料 100 万-1 亿段、每段约 500 token。1024 维 fp16 embedding = 2 KB/段，1 亿段 = 200 GB——单机 Milvus/Qdrant 加内存足够。端到端预算：检索 50ms、重排 100ms、生成 300ms 首 token 流式。</p>\n<div class="callout tip"><h4>参考来源</h4><p>Gulli《Agentic Design Patterns》第 14 章（Knowledge Retrieval）是标准目录。读原始 RAG 论文（Lewis et al. 2020）、Contriever/ColBERT、HyDE（Gao et al. 2022）。</p></div>\n\n<h2 id="s2">切块：最容易做错的地方</h2>\n<p>切块是质量最大杠杆——切得差再好的 embedding 也救不回来。三种策略：</p>\n<ul>\n  <li><strong>定长</strong>（256/512/1024 token、10-20% 重叠）。极简。叙事文本 OK；代码和结构化文档差。</li>\n  <li><strong>语义</strong>（按句/段分，再按 embedding 距离聚合相邻句）。质量最好、更费算力。</li>\n  <li><strong>结构化</strong>（按 markdown 标题、函数边界、HTML 段）。技术文档、代码、wiki 的正解。</li>\n  <li><strong>滑窗 + "父文档"检索</strong>：小块用于检索、生成时返回整段父节。两全。</li>\n</ul>\n<p>数字规则：按预期答案颗粒度调块大小。FAQ 200 token；密集技术 500-800；对话按轮。</p>\n<div class="callout warn"><h4>反模式</h4><p>一个块大小适配所有语料。一刀 512 token 把 PDF 表切一半、把代码切坏。上线前必须手看 20 份随机文档的切块结果。</p></div>\n\n<h2 id="s3">混合检索 + 重排</h2>\n<p>生产 RAG 几乎总用<strong>混合检索</strong>：</p>\n<ol>\n  <li><strong>BM25 / 关键词</strong>（Elasticsearch、OpenSearch）。抓精确匹配——SKU、错误码、人名。向量搜会错过。</li>\n  <li><strong>Dense</strong>（embedding 相似度，例如 bge-large、text-embedding-3-small 的余弦）。抓语义。</li>\n  <li><strong>RRF 倒数排名融合</strong>：<code>score(d) = Σ 1/(k + rank_i(d))</code>、k 典型 60。比单用任一种在检索 benchmark 上好 5-15%。</li>\n</ol>\n<p>然后<strong>交叉编码器重排</strong>（Cohere rerank、BGE-reranker、Jina）。对 (query, doc) 对联合打分——比双塔慢 100 倍但 NDCG 高 10-20%。流程：hybrid 召 50 → 重排 top 50 → 给 LLM top 5。</p>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  Q[用户查询] --&gt; QE[Query encoder]\n  Q --&gt; QR[Query 重写<br/>HyDE / 分解]\n  QR --&gt; BM[BM25 索引]\n  QE --&gt; VX[向量索引<br/>HNSW]\n  BM --&gt; RRF[RRF 合并]\n  VX --&gt; RRF\n  RRF --&gt; XR[交叉编码器重排]\n  XR --&gt; CTX[Top-5 chunk]\n  CTX --&gt; LLM[LLM + 引用]\n  LLM --&gt; A[答案 + refs]\n</pre></div>\n\n<h2 id="s4">Query 端技巧：HyDE、分解、多跳</h2>\n<h3>HyDE（Hypothetical Document Embeddings）</h3>\n<p>用户问句"关于带薪休假我们定了什么？"embed 效果差，因为是问句、文档是陈述句。HyDE：让 LLM <em>起草</em>一段假答案再 embed。假文档的 embedding 能更好匹配真文档。实测召回 +10-15%，代价一次 LLM 调用。</p>\n<h3>查询分解</h3>\n<p>"比较我们 Q3 和 Anthropic Q3 收入"这种多要素查询 embed 差。让 LLM 拆子查询分别检索再合并上下文。工具型 agent 标准做法；配合 planner/executor 模式（见 agentic-patterns 页）。</p>\n<h3>多跳</h3>\n<p>"收购 Figma 的公司的 CTO 是谁？"需要两次检索。方案：(a) LLM 规划器先跳 1 再跳 2；(b) GraphRAG 预先建实体图、多跳即图遍历；(c) 微调的 Self-RAG 自知何时继续检索。</p>\n\n<h2 id="s5">grounding、引用、多模态</h2>\n<h3>引用-grounded 生成</h3>\n<p>Prompt 要求 LLM 行内标 <code>[doc_id:chunk_id]</code>。后处理验证每个引用都真实存在。对 Anthropic 风格面试要说明如何<em>度量</em> grounding：训一个 NLI 模型检查输出声明是否被引用段落蕴含（FActScore、Bespoke-Minicheck 做此事）。</p>\n<h3>长上下文 vs RAG</h3>\n<p>Claude 3 Opus 20 万 token；Gemini 1.5 Pro 100 万。RAG 过时了？没有——100 万 token prompt 按 fp16 价每次 $3+，远超检索成本；"大海捞针"召回在 ~10 万 token 后衰减。长上下文<em>补充</em> RAG：粗召 10 万 token 让模型筛。</p>\n<h3>多模态 RAG</h3>\n<p>PDF 里有表、图、图片。两路：(1) OCR + 版面解析（LayoutLMv3、Unstructured.io），按表/图单独 embed 文本；(2) 视觉-语言 embedding（CLIP、SigLIP、ColPali）直接 embed 页图。ColPali 在视觉密集语料上用 late-interaction VLM 索引页图优于 OCR pipeline。</p>\n<div class="callout openai"><h4>OpenAI 细节</h4><p>OpenAI 的 Assistants/File Search 内置 RAG：自动切块（800 token、400 重叠）、混合检索、自动重排。MVP 合适；生产要失去切块和评估控制，还得自己搭。</p></div>\n<div class="callout anthropic"><h4>Anthropic 细节</h4><p>Anthropic 的 "Contextual Retrieval"（公开工程博客）在 embed 前为每个 chunk 拼一句 chunk 专属的上下文。例如："来自 2023 10-K 流动性章节：[原 chunk]"。配合 BM25 + rerank 后检索失败率减 49%。面试可引用。</p></div>\n\n<h2 id="s6">评估与面试清单</h2>\n<p>没离线评估就改不动 RAG。最小评估框架：</p>\n<ul>\n  <li><strong>检索指标</strong>：200 条 (query, gold chunk) 的 hit@k、recall@k、MRR。</li>\n  <li><strong>grounding 指标</strong>：忠实度（答案只用检索内容？）、引用准确率。</li>\n  <li><strong>端到端</strong>：LLM-as-judge 评答案质量，与 50 条人类 gold 校准。见<a href="llm-evaluation.html">评估</a>页。</li>\n  <li><strong>线上</strong>：点赞/点踩、后悔率、追问率。</li>\n</ul>\n<div class="callout warn"><h4>反模式</h4>\n<ul>\n  <li>没评估集。盲飞、所有"改进"都是感觉。</li>\n  <li>只向量检索。漏精确匹配查询。永远混合。</li>\n  <li>top-k=20 塞进 prompt。必须 cross-encoder 重排到 top-5，否则稀释 LLM。</li>\n  <li>多租户共用一索引。权限泄漏 = 合规事故。</li>\n  <li>忽略新鲜度。写时索引或小时级；每日重建 = 答案过期。</li>\n</ul></div>\n<p><strong>白板清单：</strong>语料规模与形态 → 切块策略 → embedding 模型 → 混合索引（BM25 + HNSW）→ 查询重写（HyDE / 分解）→ 交叉编码器重排 → 引用-grounded 生成 → 评估框架 → 新鲜度管线 → 按租户权限。</p>\n', 'links': [('Arena question', '真题', '../../arena/questions/o18-vector-database.html', 'O18 · Vector Database', 'O18 · 向量数据库'), ('Arena question', '真题', '../../arena/questions/o16-llm-enterprise-search.html', 'O16 · LLM Enterprise Search', 'O16 · LLM 企业搜索'), ('Previous', '上一主题', 'llm-serving.html', 'LLM Serving', 'LLM 推理'), ('Next topic', '下一主题', 'agentic-patterns.html', 'Agentic Patterns', 'Agentic 模式')]}, {'category': 'llm', 'slug': 'agentic-patterns', 'title_en': 'Agentic Patterns', 'title_zh': 'Agentic 模式', 'lead_en': "Tool use, ReAct, planner/executor, multi-agent orchestration, MCP, memory, reflection, guardrails — Gulli's canonical taxonomy, applied to OpenAI & Anthropic agents.", 'lead_zh': '工具调用、ReAct、planner/executor、多 agent 编排、MCP、记忆、反思、guardrail——Gulli 标准分类应用于 OpenAI 与 Anthropic agent。', 'tags': ['Tool use', 'ReAct', 'MCP', 'Multi-agent'], 'refs': ['Gulli Ch.1-18', 'Anthropic Building Effective Agents', 'Reflexion (Shinn 2023)'], 'toc_en': ['What counts as an agent', 'Tool use and ReAct', 'Planning, reflection, multi-agent', 'Memory: short, long, shared', 'MCP, A2A, and interop', 'Guardrails, eval, interview checklist'], 'toc_zh': ['什么算 agent', '工具使用与 ReAct', '规划、反思、多 agent', '记忆：短、长、共享', 'MCP、A2A、互操作', 'Guardrail、评估、面试清单'], 'body_en': '\n<h2 id="s1">What counts as an agent</h2>\n<p>Gulli\'s working definition: an <em>agent</em> is an LLM that can decide (1) which action to take next, (2) when to stop. Add tools + memory and you have the minimum: a loop of <code>observe → think → act</code>. Everything else — ReAct, planners, multi-agent — is a pattern on top of that loop.</p>\n<p>Three axes define every agentic system:</p>\n<ul>\n  <li><strong>Determinism</strong> — pure prompt chain (deterministic) vs LLM-chosen next step (non-deterministic). Anthropic\'s "Building Effective Agents" blog post is explicit: prefer workflows (deterministic chains) unless you genuinely need autonomy, because agents cost 4–10× more tokens and are harder to debug.</li>\n  <li><strong>Single vs multi-agent</strong> — one LLM with tools vs a team of specialized agents (planner, coder, critic). Multi-agent adds coordination cost; use it when roles are cleanly separable.</li>\n  <li><strong>Memory depth</strong> — stateless → short-term (in-context) → long-term (vector DB / summary store).</li>\n</ul>\n<p>Anchor numbers: a typical ReAct agent takes 4–12 tool calls per task; each call is 1 LLM round-trip (~2s) plus tool latency. Even a "fast" agent is 30–60s end-to-end. Budget accordingly.</p>\n<div class="callout tip"><h4>Source cross-reference</h4><p>Gulli\'s <em>Agentic Design Patterns</em> Ch.1–18 is the canonical taxonomy: prompt chaining, routing, parallelization, reflection, tool use, planning, multi-agent, memory, MCP, monitoring, exception handling, human-in-loop, RAG, A2A, resource optimization, reasoning, guardrails, evaluation. Memorize the chapter names.</p></div>\n\n<h2 id="s2">Tool use and ReAct</h2>\n<h3>Tool calling</h3>\n<p>Both Anthropic\'s <code>tool_use</code> blocks and OpenAI\'s <code>tools</code> parameter let the model emit a structured JSON call. The server executes the tool, appends the result as <code>tool_result</code>, and loops. The key technical details:</p>\n<ul>\n  <li><strong>JSON schema on tool inputs</strong>: the narrower the schema, the fewer invalid calls. Always include enum constraints and required fields.</li>\n  <li><strong>Tool descriptions matter more than names</strong>: the model chooses by description text. Write them like man pages, with examples.</li>\n  <li><strong>Parallel tool calls</strong>: both APIs support multiple tool calls per turn. Enables fan-out and is a huge latency win (e.g., 5 web searches at once = 5× faster than serial).</li>\n</ul>\n<h3>ReAct</h3>\n<p>ReAct (Yao 2022) interleaves Reasoning traces with Actions: <em>Thought → Action → Observation → Thought → ...</em>. The "thought" token is where the LLM explains why it\'s calling a tool. Empirically improves tool choice quality but burns tokens. Claude and GPT-4 both do this natively; you don\'t need a special prompt.</p>\n\n<h2 id="s3">Planning, reflection, multi-agent</h2>\n<h3>Planner/executor</h3>\n<p>For tasks with &gt;5 steps, a single-LLM ReAct loop often loses the plot. Separate the planner (expensive model, creates ordered todo) from executors (cheap models, do each task). Pattern from Plan-and-Execute (Wang 2023) and the Devin-style coding agents.</p>\n<h3>Reflection / self-critique</h3>\n<p>After the agent produces output, a "critic" LLM call reviews it against criteria and proposes fixes. Reflexion (Shinn 2023) shows 20–30% accuracy improvement on coding and reasoning benchmarks at ~2× cost. Cheap version: ask the same model to "review your answer and fix mistakes".</p>\n<h3>Multi-agent</h3>\n<p>Patterns: Supervisor-worker (one planner dispatches to specialists), Debate (two agents argue, judge decides), Society-of-Mind. <em>Interview warning</em>: multi-agent is overhyped. Many production systems called "multi-agent" are just a supervisor + tools under the hood. Don\'t recommend multi-agent unless there are genuine specialization boundaries (e.g., a coder-reviewer-tester triad, each with distinct system prompts and tools).</p>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart TB\n  U[User query] --&gt; P[Planner LLM<br/>Claude-3-Opus]\n  P --&gt; T1[Task 1]\n  P --&gt; T2[Task 2]\n  P --&gt; T3[Task 3]\n  T1 --&gt; E1[Executor<br/>Claude-3-Haiku + tools]\n  T2 --&gt; E1\n  T3 --&gt; E1\n  E1 --&gt; M[Memory: scratchpad + vector]\n  E1 --&gt; C[Critic LLM]\n  C --&gt;|revise| E1\n  C --&gt;|ok| A[Aggregator]\n  A --&gt; R[Final response]\n</pre></div>\n\n<h2 id="s4">Memory: short, long, and shared</h2>\n<h3>Short-term (in-context)</h3>\n<p>The turn buffer. Growing costs fast: Claude-3-Opus at 200k context is $15/M input tokens → $3/request for a full context. Mitigation: compress by summarization after every N turns, keep only last-K raw.</p>\n<h3>Long-term (vector + structured)</h3>\n<ul>\n  <li><strong>Episodic</strong>: past interactions indexed by embedding; retrieved by semantic query. Use vector DB (Pinecone, Milvus, pgvector).</li>\n  <li><strong>Semantic</strong>: distilled facts ("user prefers markdown"). Store as key-value with confidence scores.</li>\n  <li><strong>Procedural</strong>: learned tool sequences cached as skills.</li>\n</ul>\n<p>Writing to long-term memory is the hard part. Agents typically write too much (every turn → noise) or too little (nothing persists). Solutions: reflection-gated writes (LLM decides "worth remembering?"), TTL on low-confidence memories, periodic consolidation.</p>\n<h3>Shared memory</h3>\n<p>For multi-agent systems, a shared scratchpad (Google Doc-like) or event log lets agents coordinate. Durable store (Postgres row per thread) beats in-process maps for reliability across retries.</p>\n\n<h2 id="s5">MCP, A2A, and interop</h2>\n<h3>Model Context Protocol (MCP)</h3>\n<p>Anthropic open-sourced MCP in late 2024 as a standard for connecting LLMs to tools and data sources. Architecture: MCP servers (local or remote) expose tools, resources, and prompts; MCP clients (Claude Desktop, IDEs, agents) consume them over JSON-RPC stdio or HTTP+SSE. Wins: tool reuse across models, local filesystem access, structured resource URIs.</p>\n<p>Interview hook: MCP separates <em>capability providers</em> from <em>model runners</em>. A company wiring Claude to Jira, Slack, and S3 uses three MCP servers rather than three bespoke tool implementations inside the agent.</p>\n<div class="callout anthropic"><h4>Anthropic-specific</h4><p>MCP is an Anthropic invention. Claude Desktop and Claude Code both use MCP servers natively. Interview-level answer: MCP, combined with computer-use (screen + keyboard/mouse tools released 2024), lets Claude operate arbitrary GUI apps without per-app integrations. This is the core of Claude\'s "general agent" strategy.</p></div>\n<h3>A2A (Agent-to-Agent)</h3>\n<p>Gulli Ch.15 — standardizing agent-to-agent messaging so a planner can call a specialist across orgs. Less mature than MCP but promising for multi-company agent workflows.</p>\n<div class="callout openai"><h4>OpenAI-specific</h4><p>OpenAI\'s Assistants API provides a hosted agent runtime with threads, tools, and file search. Contrast with Anthropic\'s approach: OpenAI hides the loop inside their infra; Anthropic exposes tool-use blocks and expects you to run the loop. OpenAI\'s recent Responses API and Agents SDK narrow the gap.</p></div>\n\n<h2 id="s6">Guardrails, eval, and interview checklist</h2>\n<h3>Guardrails (Gulli Ch.18)</h3>\n<p>Two layers: <strong>input guardrails</strong> (reject prompts that violate policy, e.g., prompt injection from retrieved documents) and <strong>output guardrails</strong> (reject tool calls targeting dangerous actions, e.g., <code>rm -rf /</code>). Pattern: run a separate lightweight classifier (Llama-Guard, Anthropic\'s constitutional classifier) in parallel with the main agent. For high-stakes tools (DB writes, money transfer), require human-in-the-loop (Gulli Ch.13).</p>\n<h3>Evaluation (preview — see evaluation page)</h3>\n<p>Agent eval needs: (1) end-task success rate on a labeled suite; (2) trajectory analysis — did it take reasonable steps? (3) cost/latency; (4) failure mode taxonomy (wrong tool, loop, hallucinated citation).</p>\n<div class="callout warn"><h4>Anti-patterns</h4>\n<ul>\n  <li><strong>No max-iteration cap.</strong> Agents loop forever on ambiguous tasks. Cap to 20 steps.</li>\n  <li><strong>Unbounded tool output to context.</strong> A 2MB file read bloats context instantly. Truncate and summarize.</li>\n  <li><strong>Multi-agent by default.</strong> Start with one agent + tools; add roles only when specialization is real.</li>\n  <li><strong>No prompt-injection guard for RAG tools.</strong> A retrieved doc containing "ignore previous instructions" can hijack the agent.</li>\n  <li><strong>Stateless retries on tool failure.</strong> If Slack API returns 429, backoff and resume — don\'t replay the whole thread.</li>\n</ul></div>\n<p><strong>Whiteboard checklist:</strong> clarify autonomy vs workflow; pick loop shape (ReAct / plan-execute / multi-agent); define tools with JSON schema; short + long memory design; MCP for external capability surface; input/output guardrails; max-iteration cap; human-in-loop for high-stakes tools; eval harness with trajectory logging.</p>\n', 'body_zh': '\n<h2 id="s1">什么算 agent</h2>\n<p>Gulli 工作定义：agent 是一个能自行决定 (1) 下一步动作、(2) 何时停止的 LLM。加上工具和记忆即得最小循环：<code>observe → think → act</code>。ReAct、planner、multi-agent 都是这之上的模式。</p>\n<p>三个轴定义每个 agentic 系统：</p>\n<ul>\n  <li><strong>确定性</strong>——纯 prompt chain（确定）vs LLM 选下一步（非确定）。Anthropic "Building Effective Agents" 博文明确：除非真需要自主性，优先用 workflow（确定链），因为 agent 多花 4-10 倍 token 且更难调试。</li>\n  <li><strong>单 vs 多 agent</strong>——单 LLM + 工具 vs 专家团队（planner、coder、critic）。multi-agent 加协调成本；角色真正可分时才用。</li>\n  <li><strong>记忆深度</strong>——无状态 → 短期（上下文内）→ 长期（向量 DB / 摘要存储）。</li>\n</ul>\n<p>数字锚点：典型 ReAct agent 每任务 4-12 次工具调用；每次 1 个 LLM 往返（~2s）+ 工具延迟。"快"的 agent 也要 30-60s 端到端。按此预算。</p>\n<div class="callout tip"><h4>参考来源</h4><p>Gulli《Agentic Design Patterns》Ch.1-18 是标准分类：prompt chaining、routing、parallelization、reflection、tool use、planning、multi-agent、memory、MCP、monitoring、exception handling、human-in-loop、RAG、A2A、resource optimization、reasoning、guardrails、evaluation。背熟章节名。</p></div>\n\n<h2 id="s2">工具使用与 ReAct</h2>\n<h3>工具调用</h3>\n<p>Anthropic 的 <code>tool_use</code> 块与 OpenAI 的 <code>tools</code> 参数都让模型发结构化 JSON 调用。服务端执行工具、把结果作为 <code>tool_result</code> 追加、循环。关键技术点：</p>\n<ul>\n  <li><strong>工具输入的 JSON schema</strong>：schema 越窄无效调用越少。加 enum 约束和必填字段。</li>\n  <li><strong>工具描述比名字重要</strong>：模型按描述文字选工具。像 man page 一样写，带示例。</li>\n  <li><strong>并行工具调用</strong>：两家 API 都支持一个 turn 多调用。启用 fan-out，延迟大赢（5 次并行搜索比串行快 5 倍）。</li>\n</ul>\n<h3>ReAct</h3>\n<p>ReAct（Yao 2022）交织推理与动作：<em>Thought → Action → Observation → Thought → ...</em>。"thought" token 是 LLM 解释为何调此工具的地方。实测提升工具选择质量但耗 token。Claude 和 GPT-4 原生都这样，不用特殊 prompt。</p>\n\n<h2 id="s3">规划、反思、多 agent</h2>\n<h3>Planner/executor</h3>\n<p>&gt;5 步任务单 LLM ReAct 常跑偏。分开 planner（贵模型、列有序 todo）与 executor（便宜模型、逐项执行）。Plan-and-Execute（Wang 2023）和 Devin 风代码 agent 的模式。</p>\n<h3>反思 / 自我批评</h3>\n<p>agent 输出后，"critic" LLM 按标准审查并提出修正。Reflexion（Shinn 2023）在代码与推理 benchmark 上 +20-30% 准确率、代价 ~2×。便宜版：让同一模型"审查并修正"。</p>\n<h3>多 agent</h3>\n<p>模式：supervisor-worker（一 planner 分派专家）、debate（两 agent 争辩、judge 决定）、Society-of-Mind。<em>面试警告</em>：multi-agent 被过度吹捧。许多号称"多 agent"的生产系统本质是 supervisor + 工具。除非真有专业化边界（例如 coder-reviewer-tester 三人各有 system prompt 和工具），否则别推。</p>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart TB\n  U[用户查询] --&gt; P[Planner LLM<br/>Claude-3-Opus]\n  P --&gt; T1[任务 1]\n  P --&gt; T2[任务 2]\n  P --&gt; T3[任务 3]\n  T1 --&gt; E1[Executor<br/>Claude-3-Haiku + 工具]\n  T2 --&gt; E1\n  T3 --&gt; E1\n  E1 --&gt; M[记忆: scratchpad + 向量]\n  E1 --&gt; C[Critic LLM]\n  C --&gt;|修改| E1\n  C --&gt;|OK| A[聚合]\n  A --&gt; R[最终响应]\n</pre></div>\n\n<h2 id="s4">记忆：短、长、共享</h2>\n<h3>短期（上下文内）</h3>\n<p>turn buffer。增长极快：Claude-3-Opus 20 万上下文 $15/M 输入 token → 每请求 $3。缓解：每 N 轮摘要压缩，只保留最后 K 轮原文。</p>\n<h3>长期（向量 + 结构化）</h3>\n<ul>\n  <li><strong>情节</strong>：过去互动按 embedding 索引，语义查询召回。用向量 DB（Pinecone、Milvus、pgvector）。</li>\n  <li><strong>语义</strong>：蒸馏事实（"用户偏好 markdown"）。带置信度 KV 存储。</li>\n  <li><strong>程序性</strong>：学到的工具序列作为 skill 缓存。</li>\n</ul>\n<p>写长期是难点。Agent 要么写太多（每轮都写 → 噪声）、要么太少（啥也留不下）。方案：反思门控（LLM 判断"值得记吗？"）、低置信度加 TTL、定期合并。</p>\n<h3>共享记忆</h3>\n<p>多 agent 系统，共享 scratchpad（像 Google Doc）或事件日志让各方协调。持久化（Postgres 按 thread 一行）比内存 map 在重试时更稳。</p>\n\n<h2 id="s5">MCP、A2A、互操作</h2>\n<h3>Model Context Protocol (MCP)</h3>\n<p>Anthropic 2024 末开源 MCP 作为 LLM 与工具、数据源互联标准。架构：MCP 服务器（本地或远程）暴露 tools/resources/prompts；MCP 客户端（Claude Desktop、IDE、agent）通过 JSON-RPC stdio 或 HTTP+SSE 消费。好处：跨模型工具复用、本地文件访问、结构化资源 URI。</p>\n<p>面试点：MCP 分离<em>能力提供方</em>与<em>模型运行方</em>。把 Claude 接到 Jira、Slack、S3 的公司用三个 MCP 服务器而非在 agent 里写三份工具。</p>\n<div class="callout anthropic"><h4>Anthropic 细节</h4><p>MCP 是 Anthropic 发明。Claude Desktop 和 Claude Code 原生用 MCP。面试级答案：MCP + 计算机使用（2024 发布的屏幕 + 键鼠工具）让 Claude 无须单独集成就能操作任意 GUI 应用，是 Claude "通用 agent" 策略核心。</p></div>\n<h3>A2A (Agent-to-Agent)</h3>\n<p>Gulli 第 15 章——agent 间消息标准化，让 planner 能跨组织调专家。比 MCP 不成熟但对跨公司工作流有潜力。</p>\n<div class="callout openai"><h4>OpenAI 细节</h4><p>OpenAI Assistants API 提供托管 agent runtime（thread、tool、file search）。对比 Anthropic：OpenAI 把循环藏在基础设施里；Anthropic 暴露 tool_use 块、期望你跑循环。OpenAI 新 Responses API 与 Agents SDK 在缩小差距。</p></div>\n\n<h2 id="s6">Guardrail、评估、面试清单</h2>\n<h3>Guardrail（Gulli 第 18 章）</h3>\n<p>两层：<strong>输入 guardrail</strong>（拒策略违规 prompt，例如从检索文档来的 prompt injection）和<strong>输出 guardrail</strong>（拒危险工具调用，例如 <code>rm -rf /</code>）。模式：用独立轻分类器（Llama-Guard、Anthropic 宪法分类器）与主 agent 并行跑。高风险工具（数据库写、转账）要 human-in-the-loop（Gulli 第 13 章）。</p>\n<h3>评估（见评估页）</h3>\n<p>Agent 评估要：(1) 标注套件的端任务成功率；(2) 轨迹分析——步骤是否合理？(3) 成本/延迟；(4) 失败模式分类（错工具、循环、幻觉引用）。</p>\n<div class="callout warn"><h4>反模式</h4>\n<ul>\n  <li><strong>不设 max-iteration 上限</strong>。Agent 在模糊任务上无限循环。封 20 步。</li>\n  <li><strong>无界工具输出进上下文</strong>。读 2MB 文件瞬间涨上下文。要截断+摘要。</li>\n  <li><strong>默认 multi-agent</strong>。先单 agent+工具，只在真有专业化时加角色。</li>\n  <li><strong>RAG 工具无 prompt injection 防护</strong>。检索到的文档含"忽略先前指令"能劫持 agent。</li>\n  <li><strong>工具失败无状态重试</strong>。Slack API 429 应退避续跑，别重放整个 thread。</li>\n</ul></div>\n<p><strong>白板清单：</strong>确定性 vs workflow；循环形态（ReAct / plan-execute / multi-agent）；JSON schema 定义工具；短+长记忆设计；MCP 外部能力面；输入/输出 guardrail；max-iteration 上限；高风险工具 human-in-loop；带轨迹日志的评估框架。</p>\n', 'links': [('Arena question', '真题', '../../arena/questions/a25-agentic-system.html', 'A25 · Agentic System', 'A25 · Agentic 系统'), ('Arena question', '真题', '../../arena/questions/o11-openai-playground.html', 'O11 · OpenAI Playground', 'O11 · OpenAI Playground'), ('Previous', '上一主题', 'rag.html', 'RAG', 'RAG'), ('Next topic', '下一主题', 'llm-evaluation.html', 'LLM Evaluation', 'LLM 评估')]}, {'category': 'llm', 'slug': 'llm-evaluation', 'title_en': 'LLM Evaluation', 'title_zh': 'LLM 评估', 'lead_en': 'Offline benchmarks (MMLU, GPQA, HELM), LLM-as-judge biases, pairwise Elo arenas, guardrailed online A/B, red teaming, and agent trajectory eval.', 'lead_zh': '离线 benchmark（MMLU、GPQA、HELM）、LLM-as-judge 偏差、配对 Elo arena、带 guardrail 的线上 A/B、红队、agent 轨迹评估。', 'tags': ['MMLU/GPQA', 'LLM-as-judge', 'Arena Elo', 'Red team'], 'refs': ['Chip Huyen DMLS Ch.6', 'Gulli Ch.19', 'HELM (Liang et al.)'], 'toc_en': ['Why LLM eval is hard', 'Offline benchmarks and their limits', 'LLM-as-judge and its biases', 'Online eval, A/B, guardrails', 'Regression suites, red teaming, agent eval', 'OpenAI vs Anthropic culture, checklist'], 'toc_zh': ['为什么 LLM 评估难', '离线 benchmark 及其局限', 'LLM-as-judge 与偏差', '线上评估、A/B、guardrail', '回归套件、红队、agent 评估', 'OpenAI vs Anthropic 文化与清单'], 'body_en': '\n<h2 id="s1">Why LLM eval is hard</h2>\n<p>Classical ML eval is "compute loss on held-out test set". LLMs break this in three ways:</p>\n<ul>\n  <li><strong>Open-ended outputs</strong>. There is no single correct string — "Paris is the capital of France" and "France\'s capital is Paris" are both right. Exact-match and BLEU miss this.</li>\n  <li><strong>Behavioral surface is huge</strong>. You care not just about accuracy but helpfulness, harmlessness, instruction-following, tool-use correctness, calibration, and format compliance. Each has its own evaluator.</li>\n  <li><strong>Benchmark contamination</strong>. MMLU, HellaSwag, GSM8K have leaked into training data. Public-benchmark scores are no longer trustworthy for frontier models; you need private holdouts.</li>\n</ul>\n<p>Interview budget numbers: a single MMLU run is 14k questions ≈ $30 on GPT-4-class pricing. A serious release regression run is 100k-1M samples across ~20 benchmarks, costing thousands to tens of thousands of dollars and 4-24 hours.</p>\n<div class="callout tip"><h4>Source cross-reference</h4><p>Chip Huyen\'s <em>Designing ML Systems</em> Ch.6 covers offline eval taxonomy and slice-based eval. Gulli\'s <em>Agentic Design Patterns</em> Ch.19 covers agent eval specifically (trajectory analysis, end-task success, failure-mode taxonomy). Also worth reading: HELM (Liang et al.), MT-Bench/Arena-Hard (lmsys), and the BIG-bench paper.</p></div>\n\n<h2 id="s2">Offline benchmarks: MMLU, GPQA, HELM, and their limits</h2>\n<p>Know these by heart:</p>\n<table>\n  <thead><tr><th>Benchmark</th><th>What it measures</th><th>Size</th><th>Ceiling / SOTA</th></tr></thead>\n  <tbody>\n    <tr><td>MMLU</td><td>57 subjects multi-choice</td><td>14k</td><td>~90% (frontier); contaminated</td></tr>\n    <tr><td>GPQA</td><td>PhD-level multi-choice, Google-proof</td><td>448</td><td>~60% (o1); &lt;40% for most</td></tr>\n    <tr><td>HumanEval / MBPP</td><td>Python code synthesis</td><td>164 / 974</td><td>saturated at ~95%; use LiveCodeBench instead</td></tr>\n    <tr><td>GSM8K / MATH</td><td>Math word problems</td><td>8.5k / 12.5k</td><td>&gt;95% on GSM8K; MATH still stretching</td></tr>\n    <tr><td>HELM</td><td>Holistic, multi-metric (accuracy, calibration, robustness, fairness, bias, toxicity, efficiency)</td><td>varies</td><td>research standard</td></tr>\n    <tr><td>SWE-bench / SWE-bench Verified</td><td>Real GitHub issues resolved</td><td>2.3k / 500</td><td>~70% (Claude + agents)</td></tr>\n    <tr><td>Tau-bench, WebArena</td><td>Agent tool-use, browsing</td><td>varies</td><td>active research</td></tr>\n  </tbody>\n</table>\n<p>When a benchmark score is in a press release, <em>assume contamination</em> unless the paper reports a decontamination methodology. Frontier labs now run <strong>private holdouts</strong> (held-out test sets never public) to get trustworthy numbers.</p>\n\n<h2 id="s3">LLM-as-judge and its biases</h2>\n<p>For open-ended outputs, the standard eval is: show two responses to a judge LLM, ask which is better, aggregate pairwise preferences into an Elo score. MT-Bench and Arena-Hard (lmsys.org) do this at scale.</p>\n<p><strong>Known biases:</strong></p>\n<ul>\n  <li><strong>Position bias</strong>. The judge prefers response A over B more often when shown first. Fix: swap positions and average.</li>\n  <li><strong>Verbosity bias</strong>. Longer responses are rated higher even when wrong. Fix: length-normalize or use a length-debiased Elo (lmsys publishes one).</li>\n  <li><strong>Self-preference</strong>. A model judge prefers outputs that sound like its own. Fix: use multiple diverse judges (GPT-4 + Claude + Gemini) and average.</li>\n  <li><strong>Format sycophancy</strong>. Outputs with bullet points and headers get higher scores. Fix: instruct the judge to ignore format.</li>\n</ul>\n<p>Despite biases, LLM-as-judge correlates 0.8+ with human ratings on many tasks — good enough for regression detection, not good enough for novel capability evaluation.</p>\n<div class="callout warn"><h4>Anti-pattern</h4><p>Using GPT-4 as judge to evaluate GPT-4 outputs. Self-preference + shared failure modes. Use a different family of model as judge, or use humans for the critical slice.</p></div>\n\n<h2 id="s4">Online eval, A/B testing, guardrails</h2>\n<p>Offline is a proxy; online is truth. But LLMs make online eval harder: responses are different every time, users can\'t be blinded, and quality is subjective.</p>\n<h3>Guardrailed A/B</h3>\n<ol>\n  <li>Define <strong>north-star metric</strong> (e.g., task completion rate, user satisfaction thumbs rate).</li>\n  <li>Define <strong>guardrail metrics</strong> that must <em>not</em> regress (latency p95, cost per request, refusal rate, unsafe-output rate, format-compliance rate).</li>\n  <li>Shadow or canary 1%→10%→50% of traffic.</li>\n  <li>If north-star up AND all guardrails flat → ship.</li>\n</ol>\n<h3>Pairwise Elo (Arena)</h3>\n<p>Inside a company, build a battle arena: route 1% of traffic to <em>both</em> the candidate and baseline, show both answers to a panel of internal raters or an LLM judge, aggregate into Elo. This is how lmsys Chatbot Arena scores frontier models; internal versions at OpenAI/Anthropic follow the same pattern.</p>\n<h3>Behavioral sampling</h3>\n<p>Log a stratified random sample of production traffic for human or automated review. Stratify by tenant, prompt category, refusal, and length. 1% of production is usually enough to catch regressions within hours.</p>\n\n<h2 id="s5">Regression suites, red teaming, agent eval</h2>\n<h3>Regression suites</h3>\n<p>Every LLM team maintains a <strong>golden set</strong>: ~1k-10k prompts hand-curated to cover capability buckets (reasoning, math, code, refusals, long-context recall, tool use, multilingual, safety). Run on every model checkpoint. Diff the outputs. Any flip from correct to incorrect on a high-traffic bucket is a ship-block.</p>\n<h3>Red teaming</h3>\n<p>Adversarial eval: humans (or another LLM) try to elicit policy violations. Outputs feed back into the safety RLHF pipeline. Anthropic\'s Usage Policies and the OpenAI Model Spec both grew from red-team findings. For interviews, mention <em>Constitutional AI</em> (Bai et al.) — Anthropic\'s method of using a "constitution" (set of written principles) to generate AI-generated feedback for training.</p>\n<h3>Agent eval</h3>\n<p>Agents fail in ways single-turn eval misses: infinite loops, wrong tool chosen, tool called with wrong args, hallucinated citation. You need <strong>trajectory-level eval</strong>:</p>\n<ul>\n  <li>End-task success: did the final state match gold?</li>\n  <li>Step-level rubric: were intermediate tool calls reasonable?</li>\n  <li>Cost/latency profile: tokens and wall-time per task.</li>\n  <li>Failure taxonomy: categorize every failure into a bucket for targeted improvement.</li>\n</ul>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  M[New model checkpoint] --&gt; R[Regression suite<br/>~10k prompts]\n  M --&gt; B[Public benchmarks<br/>MMLU/GPQA/SWE-bench]\n  M --&gt; P[Private holdouts]\n  R --&gt; J[LLM-as-judge]\n  R --&gt; H[Human raters<br/>500 samples]\n  J --&gt; D[Dashboard + diff]\n  H --&gt; D\n  D --&gt; G{All guardrails green?}\n  G --&gt;|yes| C[Canary 1%]\n  G --&gt;|no| F[Fix / retrain]\n  C --&gt; O[Online A/B<br/>pairwise Elo]\n</pre></div>\n\n<h2 id="s6">OpenAI vs Anthropic eval culture, checklist</h2>\n<div class="callout openai"><h4>OpenAI-specific</h4><p>OpenAI publishes "Evals" — an open-source framework (github.com/openai/evals) with hundreds of community-contributed eval templates. Their internal process emphasizes model-spec compliance: the Model Spec is a public document that every release is scored against. Ship decisions favor aggregate capability gains even at some behavioral regression, mitigated by post-training.</p></div>\n<div class="callout anthropic"><h4>Anthropic-specific</h4><p>Anthropic\'s public safety cards (released alongside each Claude version) include detailed eval tables: capability (MMLU, GPQA, agentic), safety (BBQ, refusal-rate, jailbreak robustness), and honesty (TruthfulQA-style probes). They emphasize <em>Constitutional AI</em> and red-teaming with internal and external partners before release. Interview hook: describe how you would reproduce a mini safety card for your model change.</p></div>\n<div class="callout warn"><h4>Anti-patterns</h4>\n<ul>\n  <li><strong>Ship on MMLU +1.5%</strong>. Contamination + narrow capability. Always pair with behavioral evals.</li>\n  <li><strong>Single judge for Elo</strong>. Self-preference. Use a panel.</li>\n  <li><strong>No guardrail metrics in A/B</strong>. You\'ll regress latency, cost, or safety silently.</li>\n  <li><strong>Evaluating in isolation from product</strong>. Eval prompts should be drawn from real traffic distribution, not synthetic.</li>\n  <li><strong>No failure taxonomy</strong>. Without buckets, improvements don\'t compound.</li>\n</ul></div>\n<p><strong>Whiteboard checklist:</strong> define north-star + guardrails → offline suite (public + private + golden) → LLM-as-judge with bias fixes → human raters on stratified sample → regression gate → canary with guardrailed A/B → pairwise Elo arena → agent trajectory eval → red-team loop → public eval/safety card.</p>\n', 'body_zh': '\n<h2 id="s1">为什么 LLM 评估难</h2>\n<p>传统 ML 评估是"在留出集上算 loss"。LLM 三个方面打破这点：</p>\n<ul>\n  <li><strong>开放输出</strong>。没有单一正确字符串——"Paris is the capital of France" 和 "France\'s capital is Paris" 都对。Exact-match 和 BLEU 抓不到。</li>\n  <li><strong>行为面巨大</strong>。除准确率还要 helpfulness、无害、指令遵循、工具调用正确、校准、格式合规。每个都有评估器。</li>\n  <li><strong>Benchmark 污染</strong>。MMLU、HellaSwag、GSM8K 已混入训练数据。前沿模型的公开分不再可信，必须用私有 holdout。</li>\n</ul>\n<p>预算数：单跑 MMLU 1.4 万题 GPT-4 级约 $30。严肃发版回归要 10 万-100 万样本跨 ~20 个 benchmark，数千到数万美元、4-24 小时。</p>\n<div class="callout tip"><h4>参考来源</h4><p>Chip Huyen《Designing ML Systems》第 6 章（离线评估分类与切片评估）；Gulli《Agentic Design Patterns》第 19 章（agent 评估：轨迹分析、端任务成功、失败模式分类）。另读 HELM（Liang et al.）、MT-Bench/Arena-Hard（lmsys）、BIG-bench 论文。</p></div>\n\n<h2 id="s2">离线 benchmark：MMLU、GPQA、HELM 及其局限</h2>\n<p>要背熟：</p>\n<table>\n  <thead><tr><th>Benchmark</th><th>度量</th><th>规模</th><th>上限/SOTA</th></tr></thead>\n  <tbody>\n    <tr><td>MMLU</td><td>57 学科多选</td><td>1.4 万</td><td>~90%（前沿），已污染</td></tr>\n    <tr><td>GPQA</td><td>博士级多选、Google-proof</td><td>448</td><td>~60%（o1），多数 &lt;40%</td></tr>\n    <tr><td>HumanEval / MBPP</td><td>Python 代码合成</td><td>164 / 974</td><td>~95% 饱和，改用 LiveCodeBench</td></tr>\n    <tr><td>GSM8K / MATH</td><td>数学文字题</td><td>8.5k / 12.5k</td><td>GSM8K &gt;95%，MATH 仍有空间</td></tr>\n    <tr><td>HELM</td><td>整体多指标（准确、校准、鲁棒、公平、偏见、毒性、效率）</td><td>变</td><td>研究标准</td></tr>\n    <tr><td>SWE-bench / Verified</td><td>真 GitHub issue 解决</td><td>2.3k / 500</td><td>~70%（Claude + agent）</td></tr>\n    <tr><td>Tau-bench、WebArena</td><td>agent 工具使用、浏览</td><td>变</td><td>活跃研究</td></tr>\n  </tbody>\n</table>\n<p>新闻稿里看到的 benchmark 分，<em>默认已污染</em>——除非论文报告了去污染方法。前沿实验室现在跑<strong>私有 holdout</strong>（从未公开的测试集）拿真数。</p>\n\n<h2 id="s3">LLM-as-judge 及其偏差</h2>\n<p>开放输出评估的标准做法：把两份响应给裁判 LLM，问哪个更好，配对偏好聚合成 Elo。MT-Bench 和 Arena-Hard（lmsys.org）大规模这么做。</p>\n<p><strong>已知偏差：</strong></p>\n<ul>\n  <li><strong>位置偏差</strong>。裁判看到 A 在先时更偏 A。修法：交换位置求平均。</li>\n  <li><strong>长度偏差</strong>。更长响应即使错也评分更高。修法：长度归一化或用 lmsys 公开的长度去偏 Elo。</li>\n  <li><strong>自我偏好</strong>。模型裁判偏好像自己的输出。修法：多样裁判组合（GPT-4 + Claude + Gemini）求均。</li>\n  <li><strong>格式谄媚</strong>。带项目符号和标题的输出分更高。修法：指示裁判忽略格式。</li>\n</ul>\n<p>偏差虽在，LLM-as-judge 在很多任务上与人类评分相关 0.8+——足够抓回归，不足以评估新能力。</p>\n<div class="callout warn"><h4>反模式</h4><p>用 GPT-4 做裁判评 GPT-4 输出。自我偏好 + 共享失败模式。用不同族的模型做裁判，或对关键切片用人评。</p></div>\n\n<h2 id="s4">线上评估、A/B、guardrail</h2>\n<p>离线是代理，线上是真相。但 LLM 线上评估更难：响应每次不同、用户不盲测、质量主观。</p>\n<h3>Guardrail A/B</h3>\n<ol>\n  <li>定义 <strong>north-star 指标</strong>（任务完成率、点赞率）。</li>\n  <li>定义<em>不能</em>退化的 <strong>guardrail 指标</strong>（延迟 p95、每请求成本、拒答率、不安全输出率、格式合规率）。</li>\n  <li>影子或金丝雀 1%→10%→50%。</li>\n  <li>north-star 涨 + guardrail 全平 → 发布。</li>\n</ol>\n<h3>配对 Elo（Arena）</h3>\n<p>公司内部建 battle arena：1% 流量同时路由到候选和基线，两份答案给内部评委或 LLM 裁判看，聚合成 Elo。lmsys Chatbot Arena 给前沿模型打分就这么做；OpenAI/Anthropic 内部版同样模式。</p>\n<h3>行为采样</h3>\n<p>分层随机采样生产流量做人工或自动审查。按租户、prompt 类别、拒答、长度分层。1% 生产流量通常几小时内就能抓回归。</p>\n\n<h2 id="s5">回归套件、红队、agent 评估</h2>\n<h3>回归套件</h3>\n<p>每个 LLM 团队都有<strong>golden set</strong>：1k-10k 条人工精选 prompt 覆盖能力分桶（推理、数学、代码、拒答、长上下文、工具、多语、安全）。每个 checkpoint 跑；diff 输出；高流量桶上任何从对变错是卡发。</p>\n<h3>红队</h3>\n<p>对抗评估：人类（或另一 LLM）试图诱发策略违规。输出反馈到安全 RLHF 管线。Anthropic 使用策略与 OpenAI Model Spec 都源自红队发现。面试可提 <em>Constitutional AI</em>（Bai et al.）——Anthropic 用"宪法"（一组书面原则）生成 AI 反馈用于训练的方法。</p>\n<h3>Agent 评估</h3>\n<p>Agent 失败方式单 turn 评估抓不到：无限循环、选错工具、参数错误、幻觉引用。要<strong>轨迹级评估</strong>：</p>\n<ul>\n  <li>端任务成功：终态是否匹配 gold？</li>\n  <li>步级 rubric：中间工具调用是否合理？</li>\n  <li>成本/延迟画像：每任务 token 和墙上时间。</li>\n  <li>失败分类：每个失败归类便于针对性改进。</li>\n</ul>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  M[新模型 checkpoint] --&gt; R[回归套件<br/>~10k prompt]\n  M --&gt; B[公开 benchmark<br/>MMLU/GPQA/SWE-bench]\n  M --&gt; P[私有 holdout]\n  R --&gt; J[LLM-as-judge]\n  R --&gt; H[人类评委<br/>500 样本]\n  J --&gt; D[Dashboard + diff]\n  H --&gt; D\n  D --&gt; G{guardrail 全绿?}\n  G --&gt;|yes| C[金丝雀 1%]\n  G --&gt;|no| F[修复/再训]\n  C --&gt; O[线上 A/B<br/>配对 Elo]\n</pre></div>\n\n<h2 id="s6">OpenAI vs Anthropic 评估文化与清单</h2>\n<div class="callout openai"><h4>OpenAI 细节</h4><p>OpenAI 开源了 "Evals"（github.com/openai/evals），数百社区贡献的评估模板。内部强调对 Model Spec 合规：Model Spec 是公开文档，每次发版都要对照打分。发版决策倾向聚合能力增益即使行为上有些退化、post-training 缓解。</p></div>\n<div class="callout anthropic"><h4>Anthropic 细节</h4><p>Anthropic 发布的安全卡（每个 Claude 版本）含详尽评估表：能力（MMLU、GPQA、agentic）、安全（BBQ、拒答率、越狱鲁棒）、诚实（TruthfulQA 风格）。强调 <em>Constitutional AI</em> 与内外部伙伴红队。面试点：说明你如何为自己的模型变更复现一个迷你安全卡。</p></div>\n<div class="callout warn"><h4>反模式</h4>\n<ul>\n  <li><strong>MMLU +1.5% 就发</strong>。污染 + 能力单一。永远配行为评估。</li>\n  <li><strong>单裁判 Elo</strong>。自我偏好。用 panel。</li>\n  <li><strong>A/B 无 guardrail</strong>。延迟、成本、安全会悄悄退化。</li>\n  <li><strong>评估脱离产品</strong>。评估 prompt 要来自真流量分布，而非合成。</li>\n  <li><strong>无失败分类</strong>。没有桶，改进不叠加。</li>\n</ul></div>\n<p><strong>白板清单：</strong>定义 north-star + guardrail → 离线套件（公开 + 私有 + golden）→ LLM-as-judge 加偏差修正 → 分层样本人工评 → 回归门 → 金丝雀带 guardrail 的 A/B → 配对 Elo arena → agent 轨迹评估 → 红队循环 → 公开评估/安全卡。</p>\n', 'links': [('Previous', '上一主题', 'agentic-patterns.html', 'Agentic Patterns', 'Agentic 模式'), ('Next topic', '下一主题', 'distributed-training.html', 'Distributed Training', '分布式训练'), ('Arena question', '真题', '../../arena/questions/a29-model-serving-platform.html', 'A29 · Model Serving Platform', 'A29 · 模型服务平台'), ('Related classical', '相关经典', '../classical/rate-limiter.html', 'Rate Limiter', '限流器')]}, {'category': 'llm', 'slug': 'distributed-training', 'title_en': 'Distributed Training', 'title_zh': '分布式训练', 'lead_en': 'Data, tensor, and pipeline parallel; ZeRO/FSDP; 3D parallelism; activation checkpointing; mixed precision; fault-tolerant checkpointing at 10k+ GPU scale.', 'lead_zh': '数据、张量、流水并行；ZeRO/FSDP；3D 并行；激活 checkpointing；混合精度；万卡规模容错 checkpoint。', 'tags': ['FSDP/ZeRO-3', 'Megatron TP', '3D parallelism', 'bf16/fp8'], 'refs': ['Megatron-LM', 'DeepSpeed ZeRO', 'PyTorch FSDP'], 'toc_en': ['Why distributed training', 'Data parallelism, ZeRO, FSDP', 'Tensor and pipeline parallelism', 'Activation memory: checkpointing, sequence parallel', 'Checkpointing, resilience, and scale', 'Networking, cost, anti-patterns, checklist'], 'toc_zh': ['为什么要分布式训练', '数据并行、ZeRO、FSDP', '张量并行与流水并行', '激活内存：checkpoint、序列并行', 'checkpoint、韧性、规模', '网络、成本、反模式、清单'], 'body_en': '\n<h2 id="s1">Why distributed training</h2>\n<p>A Llama-70B model in fp16 has 140 GB of weights. Gradients are another 140 GB, optimizer states (Adam: m + v in fp32) another 560 GB — total ~840 GB <em>just for state</em>, before activations. An H100 has 80 GB HBM. You cannot train this on a single GPU. Distributed training is not optional; it\'s how every frontier model is made.</p>\n<p>Three dimensions of parallelism define the design space, and modern systems compose all three (<strong>3D parallelism</strong>):</p>\n<ul>\n  <li><strong>Data parallel (DP)</strong> — replicate the model, split the batch across replicas, all-reduce gradients.</li>\n  <li><strong>Tensor parallel (TP)</strong> — split each layer\'s matrices across devices within a node.</li>\n  <li><strong>Pipeline parallel (PP)</strong> — split the layers themselves across stages.</li>\n</ul>\n<p>Anchor numbers: training Llama-3-70B on the public recipe took ~7M H100-hours across 16k H100s for roughly two weeks. Throughput was ~400 TFLOPS/GPU sustained (out of ~990 fp16 theoretical). Model FLOPs Utilization (MFU) of 40–45% is the realistic target.</p>\n<div class="callout tip"><h4>Source cross-reference</h4><p>Key papers: <em>Megatron-LM</em> (Shoeybi 2019) for tensor parallelism, <em>GPipe</em> (Huang 2019) and <em>PipeDream</em> (Narayanan 2019) for pipeline parallelism, <em>ZeRO</em> (Rajbhandari 2020) and <em>ZeRO-Infinity</em> for optimizer-state sharding, and PyTorch\'s <em>FSDP</em> (Zhao 2023). DeepSpeed and NVIDIA Megatron-Core are the production implementations.</p></div>\n\n<h2 id="s2">Data parallelism, ZeRO, FSDP</h2>\n<h3>Classic DP</h3>\n<p>All GPUs hold a full model copy. Each processes a different micro-batch. After backward, <code>all_reduce</code> the gradients. Simple, but memory blows up: every GPU stores full weights + grads + optimizer state.</p>\n<h3>ZeRO (DeepSpeed)</h3>\n<p>Shard the state across DP ranks:</p>\n<ul>\n  <li><strong>ZeRO-1</strong>: shard optimizer states (Adam m, v). ~4× memory reduction for Adam fp32.</li>\n  <li><strong>ZeRO-2</strong>: also shard gradients. ~8× reduction.</li>\n  <li><strong>ZeRO-3</strong>: also shard weights. Only the shard needed for the current layer is gathered via <code>all_gather</code>, freed after use. Equivalent to FSDP.</li>\n</ul>\n<h3>FSDP (PyTorch)</h3>\n<p>PyTorch\'s implementation of ZeRO-3. FSDP wraps modules into units; during forward, it all-gathers a unit\'s weights, runs it, re-shards. During backward, it all-gathers again for grad computation, then reduce-scatters gradients. Communication = 1.5× all-reduce vs DDP but memory is 1/N.</p>\n<p>FSDP is the default recipe for training LLMs in 2024–2025 PyTorch. Named siblings: HSDP (Hybrid Sharded Data Parallel — shard within a node, replicate across nodes, reducing inter-node traffic).</p>\n\n<h2 id="s3">Tensor and pipeline parallelism</h2>\n<h3>Tensor parallel (TP)</h3>\n<p>Split a matrix multiply <code>Y = X @ W</code> across devices. Column-wise split of W: each device holds a column slice, outputs a column slice of Y. Row-wise split of W: each device holds a row slice, produces a partial sum; <code>all_reduce</code> to combine. Megatron-LM alternates these cleverly to minimize communication: one all-reduce per transformer block instead of two.</p>\n<p>TP is <em>intra-node</em> only in practice (NVLink at 900 GB/s between 8 GPUs). Cross-node TP (over IB at 400 Gb/s) collapses throughput. Rule: TP ≤ 8 (one node).</p>\n<h3>Pipeline parallel (PP)</h3>\n<p>Split the transformer into stages; GPU 0 has layers 0–7, GPU 1 has 8–15, etc. Micro-batches flow through like a factory line. Problem: "bubble" — the first and last micro-batches leave idle time. 1F1B (one-forward-one-backward) schedule in Megatron or interleaved schedule in PipeDream-Flush minimize bubble.</p>\n<p>PP is typically used across nodes when TP + FSDP isn\'t enough. Budget: 4–16 pipeline stages.</p>\n<h3>3D parallelism</h3>\n<p>The real recipe: TP=8 (intra-node) × PP=8 (inter-node) × DP=N (with ZeRO). A 1024-GPU cluster becomes TP8 × PP8 × DP16. This is how GPT-3, PaLM, Llama-3 were all trained.</p>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart TB\n  subgraph Node0[Node 0 - 8 H100s, TP=8]\n    G0[GPU 0 layer 0..7 slice] --- G1[GPU 1 layer 0..7 slice]\n    G1 --- G2[...]\n  end\n  subgraph Node1[Node 1 - PP stage 2, TP=8]\n    H0[GPU 0 layer 8..15] --- H1[GPU 1 layer 8..15]\n  end\n  Node0 -->|activations over IB| Node1\n  Node1 --> Next[...]\n  DP[Data-parallel replicas<br/>FSDP sharding across nodes]\n</pre></div>\n\n<h2 id="s4">Activation memory: checkpointing, sequence parallel</h2>\n<p>Even with sharded weights, <em>activations</em> (stored for backward) can dominate memory for long sequences. For a transformer at <code>seq_len=8192, batch=4, layers=80, d_model=8192, fp16</code>, activations ≈ 100+ GB per GPU.</p>\n<h3>Activation / gradient checkpointing</h3>\n<p>Don\'t store all activations; recompute during backward. Saves ~60–80% activation memory at the cost of ~30% extra FLOPs (one extra forward). Essentially mandatory for &gt;7B models.</p>\n<h3>Sequence parallel (Megatron)</h3>\n<p>LayerNorm and dropout activations are still replicated across TP ranks. Sequence parallel splits these along the sequence axis, recovering 2–4× activation memory within an existing TP group. Negligible extra communication.</p>\n<h3>Mixed precision</h3>\n<p>Train in bf16/fp16 for matmuls; keep a fp32 master copy of weights and optimizer states. Automatic Mixed Precision (AMP) is standard. On H100, fp8 training (with Transformer Engine) gives another 2× speedup with careful loss scaling — used by Nvidia/Meta/Anthropic for frontier runs.</p>\n\n<h2 id="s5">Checkpointing, resilience, and scale</h2>\n<h3>Checkpoint size</h3>\n<p>Llama-70B + optimizer state ≈ 840 GB. At 16k GPUs, a naive per-rank save is 840 GB × 16k replicas = petabytes. Real recipes:</p>\n<ul>\n  <li>Shard the checkpoint across ranks (each saves its FSDP shard).</li>\n  <li>Async / offload to CPU+NVMe while training continues.</li>\n  <li>Resume loads shards in parallel; ~2–5 minutes on a modern cluster.</li>\n</ul>\n<p>Checkpoint every 500–2000 steps. Skipping even one costs many GPU-hours if a node fails.</p>\n<h3>Failure rates</h3>\n<p>Interview number: on a 16k-GPU cluster, expect 1+ hardware failure per day (GPUs fall off the bus, NVLink link errors, node reboots). Training must tolerate this. Standard pattern:</p>\n<ul>\n  <li>Health-check ring (torch.distributed.elastic) detects dead workers.</li>\n  <li>Kill and requeue the job on remaining-healthy machines.</li>\n  <li>Resume from last checkpoint.</li>\n  <li>Meta\'s Llama 3 paper reports average of 2 interruptions per day across the run; achieved 90%+ "effective training time".</li>\n</ul>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  T[Training step] --&gt; C{step %% N == 0?}\n  C --&gt;|no| T\n  C --&gt;|yes| S[Snapshot shards<br/>each rank to local NVMe]\n  S --&gt; U[Upload to S3/GCS<br/>async]\n  F[Failure detector] --&gt; R[Restart job]\n  R --&gt; L[Load latest checkpoint<br/>from S3]\n  L --&gt; T\n</pre></div>\n\n<h2 id="s6">Networking, cost, anti-patterns, checklist</h2>\n<h3>Networking</h3>\n<p>At scale, network is the bottleneck. Modern clusters use:</p>\n<ul>\n  <li><strong>NVLink</strong> 900 GB/s intra-node (H100 NVSwitch).</li>\n  <li><strong>InfiniBand</strong> 400 Gb/s (NDR) or RoCE per GPU inter-node.</li>\n  <li><strong>Fat-tree / rail-optimized topology</strong>: each GPU has a dedicated NIC mapped to a "rail"; all-reduce across rails stays collision-free.</li>\n  <li><strong>NCCL</strong> library for collectives; topology-aware ring-allreduce or tree-allreduce.</li>\n</ul>\n<p>Communication-to-compute ratio determines MFU. ZeRO-3 adds ~50% more comm than DDP. Overlap comm with compute (NCCL streams) is crucial.</p>\n<h3>Cost</h3>\n<p>H100 at ~$3/hour on cloud. 16k GPUs × 2 weeks × $3 = $16M for a 70B pretraining run. Reserved/on-prem clusters drop this 2–3×. Efficient sharding is not a nicety — saving 10% MFU saves $1.6M.</p>\n<div class="callout openai"><h4>OpenAI-specific</h4><p>OpenAI\'s public training papers are sparse, but their GPT-4 technical report confirms massive-scale training with "predictable scaling" — extrapolating loss curves from small runs. Implied infra: custom network on Azure, mixed TP/PP/DP at ~25k GPU scale. Their RLHF pipeline uses a separate reward-model cluster in tight loop with the policy trainer.</p></div>\n<div class="callout anthropic"><h4>Anthropic-specific</h4><p>Anthropic trains on AWS Trainium + Nvidia, across multiple regions. Their safety-first approach means "preparedness" evals are run on intermediate checkpoints; unsafe capabilities may halt the run. Constitutional AI fine-tuning adds another training stage after pretraining, with RLAIF (AI-generated feedback) replacing parts of RLHF.</p></div>\n<div class="callout warn"><h4>Anti-patterns</h4>\n<ul>\n  <li><strong>TP across nodes</strong>. Inter-node IB is 10× slower than NVLink. Throughput dies.</li>\n  <li><strong>No gradient clipping</strong>. One bad batch spikes the loss and corrupts optimizer state. Always clip (1.0 is common).</li>\n  <li><strong>Fully synchronous checkpointing</strong>. Stops training for minutes per save. Use async + double-buffered.</li>\n  <li><strong>Ignoring stragglers</strong>. One slow GPU slows the whole step. Monitor per-rank step time.</li>\n  <li><strong>Optimizer in fp16</strong>. Overflows and NaNs. Keep fp32 master weights.</li>\n</ul></div>\n<p><strong>Whiteboard checklist:</strong> model size → memory budget (weights + grad + optim + act) → pick parallelism (TP=8 intra-node, PP for layer count, FSDP/ZeRO-3 for DP) → activation checkpointing + sequence parallel → bf16/fp8 mixed precision → sharded async checkpoints → health-check + requeue → NCCL comm-compute overlap → MFU target 40%+ → cost estimate.</p>\n', 'body_zh': '\n<h2 id="s1">为什么要分布式训练</h2>\n<p>Llama-70B fp16 权重 140 GB。梯度再 140 GB，优化器状态（Adam：m + v 在 fp32）再 560 GB——仅状态就 ~840 GB，还没算激活。H100 HBM 80 GB。单卡训不了。分布式训练不是可选，是前沿模型的唯一做法。</p>\n<p>三维并行定义设计空间，现代系统三者组合（<strong>3D 并行</strong>）：</p>\n<ul>\n  <li><strong>数据并行 (DP)</strong>——复制模型，batch 按副本切，all-reduce 梯度。</li>\n  <li><strong>张量并行 (TP)</strong>——每层矩阵按节点内设备切。</li>\n  <li><strong>流水并行 (PP)</strong>——按层切到不同 stage。</li>\n</ul>\n<p>锚点数：Llama-3-70B 公开食谱约 ~700 万 H100 小时、16000 H100 两周。吞吐维持 ~400 TFLOPS/GPU（理论 fp16 ~990）。MFU（模型 FLOPS 利用率）40-45% 是现实目标。</p>\n<div class="callout tip"><h4>参考来源</h4><p>关键论文：<em>Megatron-LM</em>（Shoeybi 2019）TP，<em>GPipe</em>（Huang 2019）与 <em>PipeDream</em>（Narayanan 2019）PP，<em>ZeRO</em>（Rajbhandari 2020）与 ZeRO-Infinity 优化器状态切片，PyTorch <em>FSDP</em>（Zhao 2023）。DeepSpeed 与 NVIDIA Megatron-Core 是生产实现。</p></div>\n\n<h2 id="s2">数据并行、ZeRO、FSDP</h2>\n<h3>经典 DP</h3>\n<p>所有 GPU 存满模型。各自处理不同 micro-batch。backward 后 <code>all_reduce</code> 梯度。简单，但内存爆炸：每卡存全权重 + 梯度 + 优化器状态。</p>\n<h3>ZeRO (DeepSpeed)</h3>\n<p>状态按 DP rank 切：</p>\n<ul>\n  <li><strong>ZeRO-1</strong>：切优化器状态（Adam m、v）。对 fp32 Adam 约 4× 减内存。</li>\n  <li><strong>ZeRO-2</strong>：再切梯度。~8× 减。</li>\n  <li><strong>ZeRO-3</strong>：再切权重。只在当前层需要时 <code>all_gather</code>，用完释放。等价于 FSDP。</li>\n</ul>\n<h3>FSDP (PyTorch)</h3>\n<p>PyTorch 的 ZeRO-3 实现。FSDP 把模块包成 unit；forward 时 all-gather unit 权重、运行、再切片。backward 时再 all-gather 算梯度，再 reduce-scatter 梯度。通信 = DDP 的 1.5× all-reduce，内存是 1/N。</p>\n<p>FSDP 是 2024-2025 PyTorch 训 LLM 的默认食谱。同门：HSDP（Hybrid Sharded DP——节点内切片、节点间复制，减跨节点流量）。</p>\n\n<h2 id="s3">张量并行与流水并行</h2>\n<h3>张量并行 (TP)</h3>\n<p>把矩阵乘 <code>Y = X @ W</code> 切到多设备。W 列切：每设备持一列片，输出 Y 的一列片。W 行切：每设备持一行片，产生部分和，<code>all_reduce</code> 合并。Megatron-LM 交替使用，把每 transformer block 从两次 all-reduce 降到一次。</p>\n<p>TP 实践上<em>仅在节点内</em>（NVLink 8 卡间 900 GB/s）。跨节点 TP（IB 400 Gb/s）吞吐崩。规则：TP ≤ 8（单节点）。</p>\n<h3>流水并行 (PP)</h3>\n<p>transformer 按层切 stage；GPU 0 拿 0-7 层，GPU 1 拿 8-15，等。micro-batch 像流水线流。问题："气泡"——首末 micro-batch 有空闲。Megatron 1F1B（one-forward-one-backward）或 PipeDream-Flush 交错调度减气泡。</p>\n<p>TP + FSDP 不够时跨节点上 PP。预算：4-16 个流水 stage。</p>\n<h3>3D 并行</h3>\n<p>真食谱：TP=8（节点内）× PP=8（节点间）× DP=N（带 ZeRO）。1024 卡集群 = TP8 × PP8 × DP16。GPT-3、PaLM、Llama-3 都这么训。</p>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart TB\n  subgraph Node0[节点 0 - 8 H100, TP=8]\n    G0[GPU 0 层 0..7 片] --- G1[GPU 1 层 0..7 片]\n    G1 --- G2[...]\n  end\n  subgraph Node1[节点 1 - PP stage 2, TP=8]\n    H0[GPU 0 层 8..15] --- H1[GPU 1 层 8..15]\n  end\n  Node0 -->|激活走 IB| Node1\n  Node1 --> Next[...]\n  DP[数据并行副本<br/>FSDP 跨节点切片]\n</pre></div>\n\n<h2 id="s4">激活内存：checkpoint、序列并行</h2>\n<p>即使权重切片，<em>激活</em>（backward 用）在长序列下也能吃满内存。transformer <code>seq_len=8192, batch=4, layers=80, d_model=8192, fp16</code> 时激活 ≈ 每卡 100+ GB。</p>\n<h3>激活/梯度 checkpointing</h3>\n<p>不全存激活，backward 时重算。省 60-80% 激活内存，代价多约 30% FLOPS（多一次 forward）。&gt;7B 模型基本必备。</p>\n<h3>序列并行 (Megatron)</h3>\n<p>LayerNorm 和 dropout 激活仍在 TP rank 间复制。序列并行沿序列轴切，在已有 TP 组内再省 2-4× 激活内存，额外通信可忽略。</p>\n<h3>混合精度</h3>\n<p>矩阵乘 bf16/fp16，保留 fp32 权重与优化器状态主副本。AMP 是标准。H100 上 fp8 训练（配 Transformer Engine）再 2× 加速，需谨慎 loss scaling——NVIDIA/Meta/Anthropic 的前沿 run 都用。</p>\n\n<h2 id="s5">checkpoint、韧性与规模</h2>\n<h3>checkpoint 大小</h3>\n<p>Llama-70B + 优化器状态 ≈ 840 GB。16000 卡下朴素每 rank 存 = 840 GB × 16000 = PB 级。真食谱：</p>\n<ul>\n  <li>checkpoint 按 rank 切（各存自己的 FSDP 片）。</li>\n  <li>训练继续的同时异步/卸载到 CPU+NVMe。</li>\n  <li>恢复时各片并行加载；现代集群 2-5 分钟。</li>\n</ul>\n<p>每 500-2000 步存一次。节点故障时跳过一次就是很多 GPU 小时。</p>\n<h3>故障率</h3>\n<p>面试数：16000 卡集群每日期望 1+ 硬件故障（GPU 掉线、NVLink 错、节点重启）。训练必须容忍。标准模式：</p>\n<ul>\n  <li>健康检查环（torch.distributed.elastic）检测死 worker。</li>\n  <li>杀掉并在剩余健康机上重新排入。</li>\n  <li>从最新 checkpoint 恢复。</li>\n  <li>Meta Llama 3 论文报告平均每日 2 次中断，达到 90%+ "有效训练时间"。</li>\n</ul>\n<div class="mermaid-container"><pre class="mermaid">\nflowchart LR\n  T[训练步] --&gt; C{step 能被 N 整除?}\n  C --&gt;|否| T\n  C --&gt;|是| S[快照分片<br/>各 rank 到本地 NVMe]\n  S --&gt; U[异步上传 S3/GCS]\n  F[故障检测] --&gt; R[重启作业]\n  R --&gt; L[从 S3 加载最新 checkpoint]\n  L --&gt; T\n</pre></div>\n\n<h2 id="s6">网络、成本、反模式、清单</h2>\n<h3>网络</h3>\n<p>规模下网络是瓶颈。现代集群：</p>\n<ul>\n  <li><strong>NVLink</strong> 900 GB/s 节点内（H100 NVSwitch）。</li>\n  <li><strong>InfiniBand</strong> 400 Gb/s (NDR) 或 RoCE per GPU 跨节点。</li>\n  <li><strong>Fat-tree / rail-optimized 拓扑</strong>：每 GPU 专属 NIC 映射到 "rail"，跨 rail all-reduce 无碰撞。</li>\n  <li><strong>NCCL</strong> 做集合通信，拓扑感知 ring-allreduce 或 tree-allreduce。</li>\n</ul>\n<p>通信计算比决定 MFU。ZeRO-3 比 DDP 多约 50% 通信。通信与计算重叠（NCCL stream）关键。</p>\n<h3>成本</h3>\n<p>H100 云上约 $3/小时。16000 卡 × 两周 × $3 = 70B 预训练 $1600 万。自建/保留集群降 2-3×。高效切片不是奢侈——省 10% MFU = 省 $160 万。</p>\n<div class="callout openai"><h4>OpenAI 细节</h4><p>OpenAI 训练论文少，但 GPT-4 技术报告确认了"可预测扩展"——从小规模拟合损失曲线外推。隐含基础设施：Azure 上定制网络、TP/PP/DP 混合于约 2.5 万卡规模。RLHF 管线在独立奖励模型集群与策略训练器紧耦合。</p></div>\n<div class="callout anthropic"><h4>Anthropic 细节</h4><p>Anthropic 在 AWS Trainium + Nvidia 跨区域训练。安全优先意味着"准备度"评估在中间 checkpoint 上跑，不安全能力可能中断训练。Constitutional AI 微调是预训练后的又一阶段，RLAIF（AI 生成反馈）替代部分 RLHF。</p></div>\n<div class="callout warn"><h4>反模式</h4>\n<ul>\n  <li><strong>跨节点 TP</strong>。跨节点 IB 比 NVLink 慢 10×，吞吐挂。</li>\n  <li><strong>没梯度裁剪</strong>。一个坏 batch 让 loss 飙、优化器状态腐蚀。永远 clip（1.0 常见）。</li>\n  <li><strong>全同步 checkpoint</strong>。每次存阻塞训练数分钟。用异步 + 双缓冲。</li>\n  <li><strong>忽略慢机</strong>。一卡慢拖整个 step。监控每 rank 步时间。</li>\n  <li><strong>fp16 优化器</strong>。溢出和 NaN。保 fp32 主权重。</li>\n</ul></div>\n<p><strong>白板清单：</strong>模型规模 → 内存预算（权重+梯度+优化器+激活）→ 选并行（节点内 TP=8、按层数选 PP、DP 用 FSDP/ZeRO-3）→ 激活 checkpointing + 序列并行 → bf16/fp8 混合精度 → 异步分片 checkpoint → 健康检查+重排 → NCCL 通信计算重叠 → MFU 目标 40%+ → 成本估算。</p>\n', 'links': [('Arena question', '真题', '../../arena/questions/o19-distributed-training.html', 'O19 · Distributed Training', 'O19 · 分布式训练'), ('Arena question', '真题', '../../arena/questions/o14-gpu-credit-scheduling.html', 'O14 · GPU Scheduling', 'O14 · GPU 调度'), ('Previous', '上一主题', 'llm-evaluation.html', 'LLM Evaluation', 'LLM 评估'), ('Related', '相关', 'llm-serving.html', 'LLM Serving', 'LLM 推理')]}]
